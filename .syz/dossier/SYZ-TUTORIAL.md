<!-- syz-version: 0.10.10 -->
> ⚠ Generated by syz — do not edit. Overwritten on every upgrade. Put your own notes in DOMAIN.yaml / TEST-STRATEGY.md.

# SYZYGY CLI: Master Class Tutorial

> **You never have to learn how to code to use SYZYGY.** The CLI ships all its knowledge directly to your AI code assistants. This tutorial teaches you how to author, generate, run, and debug your integration tests — giving you complete control without building from scratch.

---

## Table of Contents

- [Overview: What You'll Build](#overview-what-youll-build)
0. [Installation & Setup](#0-installation--setup)
1. [Introduction & Core Concepts](#1-introduction--core-concepts)
2. [Part 1: Capture — Define Your Domain](#2-part-1-capture--define-your-domain)
3. [Part 2: Generate — From API Spec to Runnable Plugin](#3-part-2-generate--from-api-spec-to-runnable-plugin)
4. [Part 3: Author — Write Your Test Scenarios](#4-part-3-author--write-your-test-scenarios)
5. [Part 4: Execute — Run Your Tests](#5-part-4-execute--run-your-tests)
6. [Part 5: Audit & Review — Read Your Results](#6-part-5-audit--review--read-your-results)
7. [Running Example: Complete Walkthrough](#7-running-example-complete-walkthrough)
8. [Common Workflows & Troubleshooting](#8-common-workflows--troubleshooting)
9. [Appendix: Artefact Reference — Author Anything by Hand](#9-appendix-artefact-reference--author-anything-by-hand)

---

## Overview: What You'll Build

By the end of this tutorial you'll have a working SYZYGY project that captures business requirements, generates a plugin, authors test scenarios, sends requests to a service, validates responses, and produces an auditable run report — all from plain YAML committed to git. No framework to stand up, no server to operate.

> **📦 The complete demo workspace.** Everything this tutorial builds — capture, plugin, scenarios, and reports — lives in a ready-to-run reference repo: **[github.com/syzs-code/syz-cli-demo](https://github.com/syzs-code/syz-cli-demo)**. Prefer to skip ahead, get unstuck, or compare against a finished project? Clone it and follow the **`DEMO.md`** at the repo root for a quick, guided demo:
>
> ```bash
> git clone https://github.com/syzs-code/syz-cli-demo.git
> cd syz-cli-demo
> # then follow the steps in DEMO.md
> ```
>
> The walkthrough below authors everything by hand so you learn the shape of each artefact — the repo is the destination, not a shortcut around the learning.

The path is linear. Each step produces an artefact the next step uses:

1. **Install & init** — get the `syz` CLI and lay down the `.syz/` project structure.
2. **Capture domain knowledge** — record the requirements that define correct behaviour in DOMAIN.yaml.
3. **Generate a plugin** — create procedures and templates from an OpenAPI spec with `syz generate`.
4. **Write scenarios** — compose procedures into test flows in YAML.
5. **Run & audit** — execute tests and read the ledger, debug log, and interactive HTML report.
6. **Share plugins** — install shared plugins from git and use them like local ones.

**Important:** You don't have to write any of this by hand. `syz init` creates pointer files for the AI coding assistant you already use (Claude Code, GitHub Copilot, Cursor, etc.), and `syz generate` scaffolds a whole plugin from an OpenAPI spec. This tutorial authors everything manually so you understand the shape of each artefact — once you do, let your AI assistant do the typing so you can focus on the business logic.

---

## 0. Installation & Setup

### Prerequisites

Before installing SYZYGY CLI, ensure you have:

| Requirement | Minimum Version | Why It's Needed |
|---|---|---|
| **Node.js** | 20 LTS | Runtime for SYZYGY CLI |
| **npm** | 10.x | Package manager (comes with Node.js) |
| **Git** | 2.x | To install plugins from repositories |
| **Text editor** | Any | To edit YAML files (VS Code, Sublime, etc.) |

**Operating Systems:** macOS, Linux, Windows (via WSL or native)

### Step 1: Install Node.js

SYZYGY requires **Node.js 20 LTS or later**. If you don't have it, download and install from:

👉 **https://nodejs.org/** (choose "LTS" version)

**Verify Installation:**

```bash
node --version
# Output should show: v20.x.x or higher

npm --version
# Output should show: 10.x or higher
```

### Step 2: Install SYZYGY CLI

Once Node.js is installed, install SYZYGY globally using npm:

```bash
npm install -g syzs-cli
```

**What This Does:**

- Downloads the SYZYGY CLI package from npm
- Installs it globally on your machine
- Makes the `syz` command available from any directory in your terminal

**Verify Installation:**

```bash
syz --version
# Output: e.g., "0.10.4"

syz --help
# Output: shows all available commands
```

### Step 3: Choose Your AI Assistant (Optional but Recommended)

SYZYGY works best with an AI code assistant like Claude, GitHub Copilot, Cursor, or others. The CLI automatically creates context files for:

- **Claude Code** (Web, Desktop, CLI)
- **GitHub Copilot**
- **Cursor IDE**
- **Windsurf**
- **Continue.dev**
- **Amazon Q**
- **JetBrains AI Assistant**
- **Google Gemini Code Assist**
- **Aider**

When you run `syz init` later, the CLI creates pointer files for every AI assistant you use (Claude Code, GitHub Copilot, Cursor, etc.). Open your AI tool with `.syz/` as the workspace root, and it automatically picks up the SYZYGY context.

### Step 4: Set Up Your First Project

Create a new directory for your integration tests:

```bash
# Create project folder
mkdir my-integration-tests
cd my-integration-tests

# Initialise SYZYGY workspace
syz init
```

**What `syz init` Creates:**

```
.syz/
├── dossier/                         ← Business docs + SYZYGY knowledge layer
│   ├── SYZ-INDEX.md                 ← Directive router for AI assistants (owned by SYZYGY, overwritten on init)
│   ├── SYZ-MANUAL.md                ← Full CLI manual (owned, overwritten on init)
│   ├── … other SYZ-*.md docs        ← Guidelines, testing how-to, AI-risk notice, migration playbook, plain-language playbook (owned, overwritten)
│   ├── DOMAIN.yaml                  ← Scaffolded, never overwritten
│   └── TEST-STRATEGY.md             ← Scaffolded, never overwritten
├── plugins/                         ← Your plugin definitions
├── plugins.installed/               ← Git-installed plugins (auto-gitignored)
├── scenarios/                       ← Your test scenarios (component/, integration/, e2e/, uat/, generated/)
│   └── CLAUDE.md                    ← Scenario-authoring pointer for AI assistants
├── environments/                    ← Environment configs (local, staging, prod)
├── suites/                          ← Test playlists (sample smoke.suite.yaml scaffolded)
├── results/                         ← Test results land here on each run (auto-gitignored)
├── AGENTS.md                        ← Cross-agent pointer (open agents.md standard)
├── CLAUDE.md                        ← Pointer file for Claude Code
├── GEMINI.md                        ← Pointer file for Gemini
├── CONVENTIONS.md                   ← Pointer file for Aider
├── .github/copilot-instructions.md  ← Pointer file for GitHub Copilot
├── .cursor/rules/syzygy.md          ← Pointer file for Cursor IDE
├── … more rule files                ← Windsurf, Continue.dev, Amazon Q, JetBrains AI (.windsurf/, .continue/, .amazonq/, .aiassistant/)
├── ai-api.yaml                      ← Config for AI-powered features (optional)
└── dependency.json                  ← Git-installed plugin dependencies + syz_version (the committed record of which syz set up the workspace)
```

**Important:** Explicit initialisation required. Run `syz init` once in your project root to create the `.syz/` workspace. After that, all commands (`syz run`, `syz generate`, `syz install`) require the workspace — they locate it by walking up from wherever you run them to the nearest `.syz/` (the way `git` finds a repo), so you can run them from any subdirectory of your project, including from inside `.syz/` itself. If no `.syz/` exists in the current directory or any parent, they fail fast. Everything prefixed `SYZ-` is owned by the CLI and overwritten on every `syz init`; the scaffolded files without the prefix — `.syz/README.md` (optional), `dossier/DOMAIN.yaml`, `dossier/TEST-STRATEGY.md` — are yours and are never overwritten. **What to commit:** `syz init` manages a `.gitignore` at your project root and keeps syz's own regenerable output out of git — the `SYZ-*` docs, the `AGENTS.md`/editor/`CLAUDE.md` pointer files, `plugins.installed/`, and `results/` are all rebuilt by the CLI, so they're ignored, not tracked. What you commit is what you authored: `DOMAIN.yaml`, `TEST-STRATEGY.md`, every `PLUGIN-GUIDE.yaml`, `dependency.json`, and your plugins, scenarios, environments, and suites.

**Open in Your AI Assistant:**

After running `syz init`, open the `.syz/` folder in your AI assistant:

```bash
# In VS Code (with Claude Code extension)
code .syz/

# In Cursor IDE
cursor .syz/

# In web browser (Claude at claude.ai)
# Upload the .syz/ folder or paste key files
```

Your AI assistant now has full context about SYZYGY and can help you author scenarios and plugins.

### Step 5: Verify Everything Works

Run a quick sanity check:

```bash
# Check SYZYGY version
syz --version

# List all available commands
syz --help

# Check that .syz/ structure exists
ls -la .syz/

# Validate the workspace
syz lint --env local
# (This validates syntax and resolves the environment without running any scenarios yet)
```

### Installation Troubleshooting

**Problem: `syz: command not found`**

**Solution:** Node.js or npm isn't installed, or SYZYGY wasn't installed globally.

```bash
# Reinstall with explicit global flag
npm install -g syzs-cli

# Verify the installation path
npm list -g syzs-cli
```

**Problem: `npm ERR! code EACCES` (Permission Denied)**

**Solution:** npm doesn't have permission to write to global directories.

**Option 1 — Fix npm permissions (Recommended):**

```bash
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
export PATH=~/.npm-global/bin:$PATH
npm install -g syzs-cli
```

**Option 2 — Use sudo (Not Recommended):**

```bash
sudo npm install -g syzs-cli
```

**Problem: `Node.js version too old`**

**Solution:** Upgrade to Node.js 20 LTS or later.

```bash
# On macOS (with Homebrew)
brew upgrade node

# On Windows/Linux
# Download from https://nodejs.org/
```

**Problem: `syz init` fails or creates incomplete structure**

**Solution:** Ensure you're running the latest version and have write permissions.

```bash
# Update to latest version
npm install -g syzs-cli@latest

# Verify you're in an empty directory or have write permissions
pwd
ls -la

# Try again
syz init
```

### Alternative Installation Methods

**Install Locally (Project-Specific)**

If you want SYZYGY only for one project:

```bash
cd my-integration-tests
npm install --save-dev syzs-cli

# Run via npx
npx syz --version
npx syz init
npx syz run --suite .syz/scenarios --env local
```

---

## 1. Introduction & Core Concepts

### What is SYZYGY?

SYZYGY is a tool to **capture, version, and share your integration knowledge** — and then prove it with deterministic tests. You don't start by writing test code; you start by recording what *correct* means for your integrations, in plain language, as durable requirements. Plugins, scenarios, and execution all derive from that captured knowledge and trace back to it.

### The Four-Layer Architecture

SYZYGY starts from **knowledge**, not from tests. Everything below the knowledge layer exists to realise, exercise, and run what the knowledge layer declares:

| Layer | Purpose | Who Owns It | Format |
|---|---|---|---|
| **L4 — Knowledge** | The requirements and integration behaviour that define *correct* — `DOMAIN.yaml` + each plugin's `PLUGIN-GUIDE.yaml`. **This is where SYZYGY begins.** | PO / BA / SME (domain owners) | Schema-validated YAML, plain-language `given` / `when` / `then` |
| **L3 — Test Suite** | Test scenarios, environments, execution results | QA / Test team | YAML files + generated reports |
| **L2 — Plugin** | API operations, templates, integration rules | Dev / Integration team | YAML procedures, Nunjucks templates |
| **L1 — Execution** | Protocol execution — HTTP today, more executors on the roadmap | SYZYGY runtime | Deterministic, no randomness |

Read it top-down: you author **L4** first (before a plugin or a single test exists), a plugin at **L2** realises those requirements against a real API, scenarios at **L3** exercise them, and **L1** runs everything deterministically. Coverage flows back up — tests link to L4 requirements with `covers:`, and `syz rtm` proves the loop is closed.

### Running Example: The Payments Plugin

Throughout this tutorial, we'll work with a fictional **Payments API** that manages financial transactions. Our plugin will support:

- Creating a payment (`POST /payments`)
- Retrieving payment status (`GET /payments/{id}`)
- Refunding a payment (`POST /payments/{id}/refund`)

We'll start with capturing business knowledge, an OpenAPI spec, generate a plugin, author test scenarios, run them, and review the results.

---

## 2. Part 1: Capture — Define Your Domain

### What is "Capture"?

Before you generate a plugin or write test scenarios, you need to document your business domain. This means answering questions like:

- **What is the Payments API for?** (e.g., processing customer transactions)
- **What are the business rules?** (e.g., refunds must be issued within 30 days)
- **What integrations depend on this API?** (e.g., order processing, invoicing)
- **What are the key test scenarios?** (e.g., happy path, insufficient funds, duplicate requests)

This knowledge lives in two files under `.syz/dossier/`:

1. **`DOMAIN.yaml`** — Cross-integration business knowledge
2. **`PLUGIN-GUIDE.yaml`** — Plugin-specific integration rules (one per plugin, under `.syz/plugins/payments/`)

### Understanding DOMAIN.yaml

`DOMAIN.yaml` is a **schema-validated YAML file** that captures business and technical requirements shared across your entire integration suite.

**Location:** `.syz/dossier/DOMAIN.yaml`

**Structure:**

```yaml
# What the application does end to end, in your own domain language.
summary: >
  Payments processing for customer orders — checkout, status tracking,
  refunds. Integrates with order processing and invoicing.

# The registry of domain / journey labels. A domain requirement id is
# REQ-<AREA>-DOM-<NNN>, where <AREA> must be a key declared here.
areas:
  - key: PAYMENT
    title: Payment processing
    description: charge → track → reconcile
  - key: REFUND
    title: Refunds
    description: reverse a completed payment

# The identifiers that link services together (optional)
entities:
  payment: { id: payment.id, links: [order.payment_id, invoice.payment_id] }

# What "correct" means end to end
requirements:
  - id: REQ-PAYMENT-DOM-001
    behaviour:
      given: a customer order with a valid payment method
      when: the customer pays
      then: the payment settles and the order is confirmed
    flow:                      # the journey in domain terms
      - the customer pays for the order
      - order processing charges the customer
      - invoicing reconciles the settled payment
    rules:
      - payments must be positive amounts
      - only supported currencies are accepted (AUD, CNY, EUR, GBP, INR, JPY, USD)
    notes: >
      The payment journey every other service depends on. Order processing
      charges the customer; invoicing reconciles against the settled payment.

  - id: REQ-REFUND-DOM-001
    behaviour:
      given: a completed payment
      when: a refund is requested within 30 days
      then: the payment is reversed and the order is updated
    rules:
      - refunds are only allowed on completed payments
      - a refund cannot exceed the original amount
      - duplicate refund requests for the same payment are rejected
    notes: >
      Refunds span payments and order processing — the order must reflect the
      reversal.

  - id: _REQ-PAYMENT-DOM-002   # "_" = in progress — excluded from coverage tracking
    behaviour:
      given: a payment in a non-final state
      when: the provider reports a status change
      then: the payment status is updated and downstream services are informed
    notes: >
      Status webhooks are still being designed. Drop the leading "_" once this
      is ready to be tested and tracked — it's the same requirement, so any
      covers: already pointing at it keeps working.
```

**Key Points:**

- **Business-focused, not technical.** Domain language only — no procedure, scenario, or plugin vocabulary. A Product Owner should be able to read and update this file, before a single plugin exists.
- **Schema-validated.** `syz lint` checks the shape (`id`, `behaviour`, `notes` are mandatory on every requirement), the id grammar (`REQ-<AREA>-DOM-<NNN>`, where `<AREA>` is a declared `areas:` key), and workspace-wide id uniqueness. Optional fields (`rules`, `inputs`, `errors`, `flow`, `examples`, …) come from a fixed catalog — an unrecognised key is flagged.
- **Checkable on its own, before anything else exists.** `syz lint --knowledge` validates just this layer — `DOMAIN.yaml` plus every `PLUGIN-GUIDE.yaml` — reading no environment, no scenarios and no procedures, so a workspace with clean requirements and nothing else exits 0. Every rule above still applies; the only difference is that requirements without a test yet aren't reported as gaps (with no scenarios written, that would fire on every one), and you get a count of the layer instead. That makes it the check to run *while writing requirements*, long before there's an environment to point `--env` at. `--plugin <name>` narrows it to one guide when several people are authoring in parallel.
- **Drives traceability.** Scenarios link back to these ids with `covers:`; `syz rtm` reports which requirements are covered and which are gaps.
- **Lives in `.syz/dossier/`** — scaffolded by `syz init` if absent, **never overwritten** by the CLI.
- **Shared across all plugins.** If you have multiple plugins (Orders, Payments, Invoicing), they all reference the same DOMAIN.yaml for consistency.

### Understanding PLUGIN-GUIDE.yaml

`PLUGIN-GUIDE.yaml` is a **schema-validated YAML file** that documents the integration behaviour specific to one plugin — and it ships **with** the plugin via `syz install`, so every consumer reads exactly this guide.

**Location:** `.syz/plugins/payments/PLUGIN-GUIDE.yaml`

**Structure:**

```yaml
summary: >
  Payments API — create a payment, track its status, refund it.

# The integration's behaviour, ID'd for traceability. A plugin-guide
# requirement id is REQ-PAYMENTS-PG-<NNN> — the scope matches the plugin folder.
requirements:
  - id: REQ-PAYMENTS-PG-001
    behaviour:
      given: a valid amount, currency, and customer
      when: a payment is created
      then: the payment is accepted and its identifier and status are returned
    inputs:
      - { field: amount, type: integer, required: true }    # minor units (cents)
      - { field: currency, type: string, required: true }   # ISO 4217 — AUD, CNY, EUR, GBP, INR, JPY, USD
      - { field: customer_id, type: string, required: true }
      - { field: idempotency_key, type: string, required: false }
    rules:
      - the amount must be positive
      - repeating a request with the same idempotency key returns the original result
    errors:
      - { code: 400, meaning: missing or invalid fields }
      - { code: 409, meaning: duplicate idempotency key }
      - { code: 422, meaning: business rule violated — e.g. zero amount }
    notes: >
      Creating a payment is the entry point of the integration. Payments cannot
      be modified after completion, and the service rate-limits at 100
      requests per minute.

  - id: REQ-PAYMENTS-PG-002
    behaviour:
      given: an existing payment
      when: its status is requested
      then: the current status is returned — pending, processing, completed, failed, or refunded
    inputs:
      - { field: payment_id, type: string, required: true }
    errors:
      - { code: 404, meaning: no payment exists with that identifier }
    notes: >
      Status is how downstream services observe a payment — invoicing polls it
      for reconciliation.

  - id: REQ-PAYMENTS-PG-003
    behaviour:
      given: a completed payment less than 30 days old
      when: a refund is requested
      then: the refund is accepted and the payment moves to refunded
    rules:
      - refunds are rejected on payments that are not completed
      - a refund cannot exceed the original amount
    errors:
      - { code: 422, meaning: payment not refundable — wrong state or too old }
    notes: >
      Refunds are the only mutation allowed after a payment completes.

# Binding authoring rules the AI must OBEY when building this integration's
# procedures and scenarios (optional). Unlike requirements, a guideline may name
# headers, executors, and settings — steering the mechanics is its job.
guidelines:
  - id: GL-PAYMENTS-001
    affects: "all requests to the payments service"
    rule: "requests must include the header X-Api-Version: 2"
    cause: "platform upgrade"
    impact: "absent → 400 UNSUPPORTED_VERSION"
```

**Key Points:**

- **Plugin-specific.** Each plugin gets its own PLUGIN-GUIDE.yaml in its folder, and it travels with the plugin on `syz install`.
- **No name or version fields.** The folder is the plugin's name and `plugin.yaml` holds the version — never duplicated here.
- **Same validation as DOMAIN.yaml.** `id`, `behaviour`, `notes` mandatory; id grammar `REQ-<PLUGIN>-PG-<NNN>`; `syz lint` enforces it. The `_REQ-…` in-progress marker works here too.
- **Requirements stay in domain language; guidelines carry the mechanics.** A requirement never names headers or executors — a binding authoring rule that must (an API version header, a TLS setting) goes under `guidelines:`.
- **Never overwritten.** Scaffolded by `syz generate plugin` if absent, **always preserved** — even with `--force`.
- **The guide can exist before the plugin does.** `syz generate plugin --name <name> --guide-only` scaffolds a knowledge-only shell — the guide, an identity `plugin.yaml`, and a `CLAUDE.md` pointer, nothing else — so behaviour is recorded before any endpoint or spec exists. `syz lint` and `syz rtm` pick the requirements up immediately.

### Complete field reference

Both files share the same building blocks — `requirements:` and `guidelines:` — and only a small core of each is mandatory. Everything else is optional and drawn from a **fixed catalog**: pick a field from the menu below rather than inventing one. An unrecognised key isn't a hard error — `syz lint` flags it as an `UNKNOWN_KNOWLEDGE_FIELD` warning — but the catalog is what keeps these files consistent across a team. When nothing in the catalog fits, put it under `more_details:` (a requirement's single free-form escape hatch), not a new top-level key.

**A note on enforcement.** The columns below mark whether `syz lint` **checks the shape** of a field (✓) or only recognises the name and leaves the structure to you (guidance). Only `id`, `behaviour` (with its `given` / `when` / `then`), and `notes` have their shape enforced on a requirement; the rest accept any structure, so the shapes shown are the recommended convention, not a contract.

#### Top-level keys

| File | Key | Required? | Holds |
|---|---|---|---|
| Both | `summary` | Optional | One line describing the file's scope |
| DOMAIN only | `areas` | Optional | Registry of domain/journey labels — a `-DOM-` id's scope must be a declared `areas[].key` |
| DOMAIN only | `entities` | Optional | The identifiers that link services (e.g. `payment.id` ↔ `order.payment_id`) |
| Both | `requirements` | Optional | The list of behaviours (the **WHAT** — tested via `covers:`) |
| Both | `guidelines` | Optional | Binding authoring rules (the **HOW** — obeyed, never tested) |

> `PLUGIN-GUIDE.yaml` has **no** `areas` or `entities` (those are cross-integration, so DOMAIN-only), and **neither** file carries a `plugin` or `version` field — the folder is the name and `plugin.yaml` is the version.

#### `requirements[]` — every field (identical in both files)

| Field | Sub-field | Required? | Shape enforced? | Value / convention |
|---|---|---|---|---|
| `id` | | **Mandatory** | ✓ | `REQ-<SCOPE>-<PG\|DOM>-<NNN>`; optional leading `_` marks a draft. `PG` scope = plugin folder, `DOM` scope = an `areas[].key` |
| `behaviour` | | **Mandatory** | ✓ | The Given/When/Then block |
| `behaviour` | `given` | **Mandatory** | ✓ | Assumed context/state — a string **or** a list of strings |
| `behaviour` | `when` | **Mandatory** | ✓ | The single action under test |
| `behaviour` | `then` | **Mandatory** | ✓ | The observable outcome(s) — string or list |
| `notes` | | **Mandatory** | ✓ | Human-facing narrative (min. 1 char); these files have no rendered view, so humans read `notes:` directly |
| `summary` | | Optional | guidance | Short one-line description |
| `inputs` | | Optional | guidance | e.g. `- { field: amount, type: integer, required: true }` |
| `outputs` | | Optional | guidance | What the operation returns |
| `preconditions` | | Optional | guidance | State required before the action (alternative to putting it in `given`) |
| `rules` | | Optional | guidance | List of domain constraints the behaviour enforces |
| `effects` | | Optional | guidance | Side effects the action produces |
| `postconditions` | | Optional | guidance | State guaranteed after the action |
| `lifecycle` | | Optional | guidance | Legal states / transitions of the resource |
| `errors` | | Optional | guidance | e.g. `- { code: 409, meaning: duplicate idempotency key }` |
| `data` | | Optional | guidance | Reference data / value sets |
| `examples` | | Optional | guidance | Concrete example values |
| `references` | | Optional | guidance | Links to specs, tickets, docs (note: **plural** on a requirement) |
| `flow` | | Optional | guidance | **DOMAIN only in practice** — the cross-service journey in domain terms |
| `more_details` | | Optional | ✓ (map) | The single escape hatch: a free-form map for anything outside the catalog |

> **`errors[]` (and its `code:`) is documentation, not a contract.** `errors` is a *guidance* field: `syz lint` recognises the top-level key but **never parses its contents**, and nothing in execution reads them — a validate procedure asserts an HTTP status against `$.fields.status_code`, completely independently of what you write here. `code:` is **free-form**: a bare HTTP status (`code: 400`), a labelled string (`code: HTTP_400`), or an application error code (`code: INSUFFICIENT_FUNDS`) — pick one convention and keep it consistent across your knowledge files.

#### `guidelines[]` — every field (identical in both files)

| Field | Required? | Shape enforced? | Value / convention |
|---|---|---|---|
| `id` | **Mandatory** | ✓ | `GL-<SCOPE>-<NNN>` (no `PG`/`DOM` marker — guidelines carry no `covers:`) |
| `rule` | **Mandatory** | ✓ | The rule to obey when building procedures/scenarios (min. 1 char) |
| `affects` | Optional | ✓ | What the rule applies to |
| `cause` | Optional | ✓ | Why the rule exists (e.g. a platform upgrade) |
| `impact` | Optional | ✓ | What happens if it's ignored |
| `references` | Optional | ✓ | Link(s) to a ticket/doc — a string or a list of strings |

#### `areas[]` — DOMAIN.yaml only

| Field | Required? | Shape enforced? | Value |
|---|---|---|---|
| `key` | **Mandatory** | ✓ | Uppercase token; the `<SCOPE>` half of a `-DOM-` requirement id |
| `title` | Optional | ✓ | Human-readable label |
| `description` | Optional | ✓ | One-line description of the journey/capability |

> **Worth knowing:** `references` is spelled the same (plural, string-or-list) on both a requirement and a guideline, so you never have to remember which form a given field wants. And `flow:` is structurally accepted on any requirement but is only meaningful in `DOMAIN.yaml`, where the journey spans services.

### How to Create These Files

You have three options:

**Option 1: Guide-First Scaffold (plugin guides, before any spec exists)**

```bash
# Scaffold a knowledge-only plugin shell — no API spec needed
syz generate plugin --name payments --guide-only

# Fill in .syz/plugins/payments/PLUGIN-GUIDE.yaml with requirements
# (yourself, or with your AI assistant — see Option 3)

# Later, when the spec exists, build the runnable plugin on the same
# folder — your guide is preserved:
syz generate plugin --name payments --from openapi:./payments.json
```

`--guide-only` refuses to touch an existing plugin folder, and can't be combined with `--from`, `--force`, or `--overwrite` — it only ever creates a fresh shell.

**Option 2: Manual Creation**

```bash
# Run syz init to scaffold the structure
syz init

# Edit .syz/dossier/DOMAIN.yaml manually
# Edit .syz/plugins/payments/PLUGIN-GUIDE.yaml manually
```

**Option 3: Ask Your AI Assistant**

Open `.syz/CLAUDE.md` or `.syz/.github/copilot-instructions.md` in your AI assistant (Claude, Copilot, etc.) and ask:

> "Write DOMAIN.yaml for our Payments API integration — declare the areas, the entities that link our services, and the end-to-end requirements. Follow the authoring rules in dossier/SYZ-GUIDELINES.md."

The AI has the full SYZYGY context and will generate properly formatted files.

---

## 3. Part 2: Generate — From API Spec to Runnable Plugin

### What is "Generate"?

After you've documented your domain, the next step is to **generate a plugin from your API specification**. SYZYGY reads an OpenAPI (Swagger) or Postman collection and deterministically creates:

- **Send procedures** (one per operation — a structured HTTP request definition)
- **Validate procedures** (a status-check validate per operation, plus a shared `validate_send_no_error`)
- **Workflow procedures** (a `<operation>_send_success` workflow per operation, composing the send with the no-error validate)
- **data.yaml** (example test data per operation — request bodies and query params)
- **Sample scenarios** (one runnable scenario per operation, tagged `sample, smoke`)
- **Environment seeding** (`.syz/environments/local.yaml`, wired with each step's `base_url` and `auth_token`)

*(Nunjucks craft templates are only generated for multipart/binary upload operations — plain JSON operations carry their request in structured fields, no template involved.)*

### The `syz generate` Command

**Basic Syntax:**

```bash
syz generate plugin --name <plugin-name> --from openapi:<path> [options]
```

**Full Signature:**

```bash
syz generate plugin \
  --name payments \
  --from openapi:./payments-api.openapi.yaml \
  --force \
  --no-install
```

### Command Flags Explained

| Flag | Purpose | Default |
|---|---|---|
| **Both modes** | | |
| `--name` | Plugin folder name | Required |
| `--dry-run` | Print files that would be written; write nothing | `false` |
| **Mode A — Spec / prompt** *(default · builds procedures, env seeding, scenarios)* | | |
| `--from` | Source of operations; omit for the interactive walkthrough | `prompt` |
| &nbsp;&nbsp;↳ `openapi:<path>` | Operations auto-discovered from an OpenAPI spec (YAML or JSON) | |
| &nbsp;&nbsp;↳ `postman:<path>` | Operations auto-discovered from a Postman collection (JSON) | |
| &nbsp;&nbsp;↳ `prompt` | Interactive walkthrough — you type each operation by hand | *(default)* |
| &nbsp;&nbsp;↳ `--yes` | Skip the **"Generate all N ops?"** prompt — takes every discovered operation. Also auto-approves the driver-install prompt. No effect on the `prompt` walkthrough. Implied on non-TTY stdin. | `false` |
| &nbsp;&nbsp;↳ `--no-install` | Disable on-demand driver provisioning; fail fast with the exact install command | `false` |
| &nbsp;&nbsp;↳ `--force` | Wipe and regenerate the whole plugin (preserves `PLUGIN-GUIDE.yaml` and `README.md`) | `false` |
| &nbsp;&nbsp;↳ `--overwrite` | Replace only selected operations; preserve all others | `false` |
| **Mode B — Guide-only** | | |
| `--guide-only` | Scaffold a knowledge shell only: `PLUGIN-GUIDE.yaml` + `plugin.yaml` + `CLAUDE.md`. `--from`, `--force`, and `--overwrite` are rejected. Target folder must not already exist. | `false` |

### Step-by-Step: Generating the Payments Plugin

**Step 1: Prepare Your OpenAPI Spec**

Create `payments-api.openapi.yaml`:

```yaml
openapi: 3.0.0
info:
  title: Payments API
  version: 1.0.0
servers:
  - url: https://api.payments.example.com/v1

paths:
  /payments:
    post:
      operationId: createPayment
      summary: Create a new payment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount, currency, customer_id]
              properties:
                amount:
                  type: number
                  description: Amount in minor units (cents)
                  example: 1000
                currency:
                  type: string
                  description: ISO 4217 currency code
                  example: GBP
                customer_id:
                  type: string
                  example: cust_123
      responses:
        '201':
          description: Payment created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  status:
                    type: string
                  amount:
                    type: number

  /payments/{id}:
    get:
      operationId: getPaymentStatus
      summary: Retrieve payment details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Payment details
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  status:
                    type: string
                  amount:
                    type: number
```

**Step 2: Run the Generator**

```bash
cd /path/to/project
syz generate plugin --name payments --from openapi:./payments-api.openapi.yaml
```

**What Gets Created:**

```
.syz/
├── plugins/
│   └── payments/
│       ├── plugin.yaml
│       ├── PLUGIN-GUIDE.yaml          ← (scaffolded, not overwritten)
│       ├── CLAUDE.md                  ← pointer to PLUGIN-GUIDE.yaml
│       ├── procedures/
│       │   ├── send_create_payment.yaml              ← POST /payments (send)
│       │   ├── validate_create_payment.yaml          ← status check for POST /payments
│       │   ├── create_payment_send_success.yaml      ← workflow: send + no-error validate
│       │   ├── send_get_payment_status.yaml          ← GET /payments/{id} (send)
│       │   ├── validate_get_payment_status.yaml
│       │   ├── get_payment_status_send_success.yaml
│       │   └── validate_send_no_error.yaml           ← shared no-error validate
│       ├── models/
│       │   └── validate_send_no_error_input.yaml
│       └── data.yaml                  ← example test data per operation
├── scenarios/
│   └── generated/
│       └── payments/
│           ├── sample_create_payment.yaml
│           └── sample_get_payment_status.yaml
├── environments/
│   └── local.yaml                     ← seeded with base_url + auth_token per step
```

**Step 3: Review Generated Files**

Open the generated procedure and template files to understand what was created:

```bash
# See the send procedure definition
cat .syz/plugins/payments/procedures/send_create_payment.yaml

# See the generated test data
cat .syz/plugins/payments/data.yaml

# See the example scenario
cat .syz/scenarios/generated/payments/sample_create_payment.yaml
```

### Generator Workflow Scenarios

**Scenario 1: Fresh Generation**

```bash
# First time? Use this:
syz generate plugin --name payments --from openapi:./api.yaml
```

The generator creates all files. Your PLUGIN-GUIDE.yaml is scaffolded with placeholders.

**Scenario 2: API Updated — Regenerate Everything**

```bash
# API spec changed significantly? Regenerate from scratch:
syz generate plugin --name payments --from openapi:./api.yaml --force

# This DELETES and recreates procedures/, models/, templates/, data.yaml
# ⚠️  Your PLUGIN-GUIDE.yaml is preserved (never deleted)
# ⚠️  Your hand-authored procedures are lost — use --overwrite instead
```

**Scenario 3: API Updated — Preserve Custom Work**

```bash
# API has 5 operations. You customised 2. Generate only new ones:
syz generate plugin --name payments --from openapi:./api.yaml --overwrite

# "Which operations do you want to regenerate?" Pick only the new ones. --overwrite touches nothing
# outside your selection — so customised procedures on the operations you leave unselected stay exactly
# as you wrote them. (Select an operation you'd edited, and it gets regenerated too.)
```

**Scenario 4: Preview Before Writing**

```bash
# What would be generated? Check without writing:
syz generate plugin --name payments --from openapi:./api.yaml --dry-run

# Output shows file paths and operations found, but creates nothing
```

### Generated Artefact Details

#### Generated Send Procedure (send_create_payment.yaml)

```yaml
name: payments.send_create_payment
type: send
execution_library: http
description: "Create a new payment"

outputs:
  id:
    path: "$.fields.body.id"
    description: "ID of the created payment"
  status:
    path: "$.fields.body.status"
    description: "Current payment status"

http:
  request:
    fields:
      base_url: "{{ env.payments.send_create_payment.base_url }}"
      method: POST
      path: /payments
      body:
        type: raw
        content: "{{ plugin.payments.data.create_payment.body }}"
    headers:
      Authorization: "Bearer {{ env.payments.send_create_payment.auth_token }}"

sensitive_fields:
  - http.request.headers.Authorization  # bearer token is masked in logs
```

No hand-written HTTP code and no template: the request is a **structured block**. `base_url` and `auth_token` are wired to the seeded environment file, and the example body lives in the plugin's `data.yaml`. A path parameter (as in `GET /payments/{id}`) is emitted as a `{{ ctx.var.id }}` read — your scenario supplies the value with a `set:` block on an earlier step.

#### Generated Validate Procedure (validate_create_payment.yaml)

```yaml
name: payments.validate_create_payment
type: validate
description: "Validates the createPayment response status"
checks:
  - type: http
    description: "HTTP status matches expected"
    path: "$.fields.status_code"
    operator: equals
    value_from_input: expected_status
```

#### Generated Workflow Procedure (create_payment_send_success.yaml)

```yaml
name: payments.create_payment_send_success
type: workflow
description: "createPayment happy path — send, then check no transport error"
steps:
  - procedure: payments.send_create_payment
    inputs: {}
  - procedure: payments.validate_send_no_error
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
```

#### Generated Sample Scenario (sample_create_payment.yaml)

```yaml
name: Sample createPayment scenario
level: e2e
plugins:
  - name: payments
    version: 1.0.0
tags: [sample, smoke]
flow:
  - type: send
    procedure: payments.send_create_payment
    inputs: {}
  - type: validate
    procedure: payments.validate_create_payment
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
      expected_status: 201
```

#### Seeded Environment (environments/local.yaml)

```yaml
name: local
payments:
  send_create_payment:
    base_url: https://api.payments.example.com/v1
    auth_token: "{{ os-env-var:PAYMENTS_AUTH_TOKEN }}"
  send_get_payment_status:
    base_url: https://api.payments.example.com/v1
    auth_token: "{{ os-env-var:PAYMENTS_AUTH_TOKEN }}"
```

Both entries name the **same** OS variable, and the repetition is deliberate: one plugin is one upstream system and therefore one credential set, so the seeded name is `<PLUGIN>_<FIELD>` — plugin-scoped, not per-operation. **One export runs every scenario for the plugin**, and rotating the credential is one change rather than one per operation. If a particular operation genuinely needs its own credential, point that one entry at a different variable and export it.

If `local.yaml` already exists, the generator only **adds** missing keys — it never rewrites values you've set.

---

## 4. Part 3: Author — Write Your Test Scenarios

### What is "Author"?

Once your plugin is generated, you write **test scenarios** — YAML files that describe a sequence of steps: send a request, validate the response, send another request, etc.

A scenario mirrors a **real workflow** in your system. For example:

1. Create a payment (send)
2. Check the payment status (send)
3. Verify the response (validate)
4. Refund the payment (send)
5. Verify the refund (validate)

### Scenario YAML Structure

**Location:** `.syz/scenarios/e2e/payment_flow.yaml`

**Basic Template:**

```yaml
# Scenario identity
name: "Payment Flow — Happy Path"
level: e2e

# Which plugins does this scenario use?
plugins:
  - name: payments
    version: "1.0.0"

# Connection handles (for stateful executors; optional for HTTP)
connections: {}

# The actual test flow
flow:
  # Step 1: Create a payment
  - type: send
    procedure: payments.send_create_payment
    inputs:
      amount: 5000        # £50.00
      currency: "GBP"
      customer_id: "cust_alice"

  # Step 2: Verify the payment was created
  - type: validate
    procedure: payments.validate_payment_created
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
      expected_status: 201

  # Step 3: Retrieve the payment status
  - type: send
    procedure: payments.send_get_payment_status
    inputs:
      id: "{{ ledger.payments.send_create_payment.outputs.id }}"

  # Step 4: Verify the status is "completed"
  - type: validate
    procedure: payments.validate_payment_status
    inputs:
      response: "{{ ledger.payments.send_get_payment_status.http.response }}"
      expected_status: "completed"
```

> **Where do `inputs:` land?** A freshly generated send procedure posts the example body from `data.yaml` and needs no inputs (`inputs: {}`). To drive values from the scenario, edit the send procedure's request block to reference inputs — e.g. `content: { "amount": "{{ inputs.amount }}", "currency": "{{ inputs.currency }}" }`. Each step's `inputs:` map merges over the procedure's `defaults:` before the step runs; inside the procedure, every key is available as `{{ inputs.<name> }}`.

> **The validate procedures are yours to author.** `payments.validate_payment_created` and `payments.validate_payment_status` (referenced below) are **not** produced by `syz generate` — it emits a status-only `validate_create_payment`. Author the richer ones yourself (see [§7 Step 5b](#step-5b-adapt-the-generated-plugin-wire-inputs-add-validate-checks)); a scenario that references a procedure with no file fails with an unknown-procedure error.

### Understanding Step Types

#### Type 1: Send Steps

A send step **makes a request** to your API or system.

```yaml
- type: send
  procedure: payments.send_create_payment
  inputs:
    amount: 5000
    currency: "GBP"
    customer_id: "cust_alice"
```

**Key Points:**

- The procedure name (`payments.send_create_payment`) maps to a YAML file in `.syz/plugins/payments/procedures/`
- `inputs` are the concrete values passed to the request template
- The response is automatically captured in the ledger
- A send step **never** stops the run on its own — it just captures the response (success, error, both)

**Referencing Previous Responses:**

Use `{{ ledger.<plugin>.<procedure>... }}` to reference previous step results. A send step's request/response live under its executor key — `{{ ledger.<plugin>.<procedure>.http.response... }}` — and any declared output is available via the `{{ ledger.<plugin>.<procedure>.outputs.<name> }}` shortcut:

```yaml
# Create a payment first
- type: send
  procedure: payments.send_create_payment
  inputs:
    amount: 5000
    currency: "GBP"
    customer_id: "cust_alice"

# Then get its status using the ID from the create response
- type: send
  procedure: payments.send_get_payment_status
  inputs:
    id: "{{ ledger.payments.send_create_payment.outputs.id }}"
```

#### Type 2: Validate Steps

A validate step **runs assertions** on a previous response.

```yaml
- type: validate
  procedure: payments.validate_payment_created
  inputs:
    response: "{{ ledger.payments.send_create_payment.http.response }}"
    expected_status: 201
```

**Key Points:**

- Assertions check the response against expectations
- If an assertion fails, the scenario **stops** (unless `--continue-scenario-on-failure` is set)
- The validate procedure defines the checks in its YAML

**Example Validate Procedure:**

```yaml
name: payments.validate_payment_created
type: validate
description: "Validates a successful payment creation"

input_model: models/validate_payment_created_input.yaml
checks:
  - type: http
    description: "Response status is 201"
    path: "$.fields.status_code"
    operator: equals
    value_from_input: expected_status

  - type: json
    description: "Payment ID is present"
    path: "$.fields.body.id"
    operator: exists

  - type: json
    description: "Status is 'pending' or 'processing'"
    path: "$.fields.body.status"
    operator: in
    value: ["pending", "processing"]

  - type: json
    description: "Amount matches request"
    path: "$.fields.body.amount"
    operator: equals
    value_from_input: expected_amount
```

**Assertion Operators:**

| Operator | Type | Purpose | Example |
|---|---|---|---|
| `equals` | json, text, http | Exact match | `status` equals `"completed"` |
| `not_equals` | json, text, http | Not a match | `error` not_equals `null` |
| `exists` | json, text, http | Field or value exists | `id` exists |
| `not_exists` | json, http | Field or value is missing | `error` not_exists |
| `contains` | json, text | Substring or array member | `message` contains `"success"` |
| `not_contains` | json, text | Substring or array member absent | `message` not_contains `"error"` |
| `in` | json, http | Value is one of a list | `currency` in `["GBP", "USD"]` |
| `not_in` | json, http | Value is NOT one of a list | `status` not_in `["failed", "aborted"]` |
| `greater_than` | json, http | Numeric comparison: value > expected | `amount` greater_than `0` |
| `less_than` | json, http | Numeric comparison: value < expected | `age` less_than `100` |
| `greater_than_or_equal` | json, http | Numeric comparison: value ≥ expected | `score` greater_than_or_equal `80` |
| `less_than_or_equal` | json, http | Numeric comparison: value ≤ expected | `retries` less_than_or_equal `3` |
| `matches` | json, text | Regex pattern match | `id` matches `^[a-f0-9]{32}$` |
| `matches_schema` | json | Validate against JSON schema | validates JSON structure |
| `count_equals` | json | Array length or object key count | `items` count_equals `5` |
| `all` | json | All array elements match condition | array validation (advanced) |
| `any` | json | At least one array element matches | array validation (advanced) |
| `expression` | json | Execute custom JavaScript expression | advanced JavaScript (sandboxed) |
| `ai` | json, text, http | AI-powered semantic assertion | `message` ai `"Does this confirm success?"` |

#### Type 3: Workflow Steps

A workflow step **runs a pre-defined workflow** (a reusable sequence of send + validate steps).

```yaml
- type: workflow
  procedure: payments.workflow_full_payment_cycle
  overrides:
    - procedure: payments.send_create_payment
      inputs:
        amount: 5000
        currency: "GBP"
```

A workflow step takes **no direct `inputs:`** — each child step's inputs are declared inside the workflow procedure itself, and the calling scenario customises them per child with `overrides:` (each entry names the child procedure via `procedure:`; override keys win field-by-field over the workflow's own values).

Workflows are useful when you have a common sequence (e.g., "create payment → validate → get status") that multiple scenarios use.

#### Type 4: Craft Steps

A craft step **generates content** (XML, JSON, etc.) using templates or AI.

```yaml
- type: craft
  procedure: accounting.craft_invoice_xml
  inputs:
    payment_id: "{{ ledger.payments.send_create_payment.outputs.id }}"
    amount: 5000
  set:
    invoice_xml: "{{ ledger.accounting.craft_invoice_xml.response.body.data }}"
```

### Advanced: Context Variables (`ctx.var`)

Use `ctx.var` to store and reference values across steps, especially when the source is unknown or shared.

```yaml
flow:
  # Capture the payment ID
  - type: send
    procedure: payments.send_create_payment
    inputs:
      amount: 5000
      currency: "GBP"
      customer_id: "cust_alice"
    set:
      payment_id: "{{ ledger.payments.send_create_payment.outputs.id }}"

  # Use the stored ID later
  - type: send
    procedure: payments.send_get_payment_status
    inputs:
      id: "{{ ctx.var.payment_id }}"

  # Sensitive values are masked run-wide
  - type: send
    procedure: auth.send_login
    set:
      auth_token: "{{ ledger.auth.send_login.http.response.fields.body.token }}"
    sensitive_fields:
      - ctx.var.auth_token
```

### Advanced: Scenario Outlines (Parameterised Tests)

Run the same flow with different inputs.

```yaml
name: "Payments — Parameterised Amounts"
level: e2e

plugins:
  - name: payments
    version: "1.0.0"

flow:
  # Flow references outline variables via {{ outline.fieldName }}
  - type: send
    procedure: payments.send_create_payment
    inputs:
      amount: "{{ outline.amount }}"
      currency: "{{ outline.currency }}"
      customer_id: "cust_alice"

  - type: validate
    procedure: payments.validate_payment_created
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
      expected_status: "{{ outline.expected_status }}"

# One execution per row
examples:
  - name: "standard-order"
    amount: 5000
    currency: "GBP"
    expected_status: 201

  - name: "zero-amount-rejected"
    amount: 0
    currency: "GBP"
    expected_status: 400

  - name: "large-usd-order"
    amount: 500000
    currency: "USD"
    expected_status: 201

  - name: "invalid-currency"
    amount: 5000
    currency: "XXX"
    expected_status: 422
```

**Results Structure:**

```
.syz/results/run-2026-07-04T10-53-00-000Z/
└── e2e/
    └── payment_parameterized/
        ├── standard-order/
        │   └── ledger.json
        ├── zero-amount-rejected/
        │   └── ledger.json
        ├── large-usd-order/
        │   └── ledger.json
        └── invalid-currency/
            └── ledger.json
```

Each example runs **in isolation** — one failure doesn't affect others.

### Advanced: Connections (Named Connection Handles)

A scenario can declare named **connection handles** in a `connections:` block and point steps at them via `connection:`. For HTTP — the only executor shipped today — handles are optional (each send step already carries its full request definition), but they let you centralise shared settings:

```yaml
name: "Payment Processing — Shared Connection"
level: integration

plugins:
  - name: payments
    version: "1.0.0"

connections:
  payments_api:
    execution_library: http
    lifecycle: keep_alive        # keep_alive | ephemeral (default)

flow:
  - type: send
    procedure: payments.send_create_payment
    connection: payments_api
    inputs: {}
```

**Roadmap note:** stateful executors (SQL databases, Kafka, message queues, SFTP, …) are planned but **not shipped yet** — today only `execution_library: http` runs. When they land, the same handle shape will carry their connection identity (host, credentials, dialect), opened once, reused across steps, and torn down at scenario end.

### Advanced: Poll Until Converged (`poll:`)

Some state becomes true only *after* a delay — an async settlement lands, a downstream consumer catches up, a row materialises. A fixed `syz.wait` guesses the duration and is a flakiness smell; a `poll:` block instead **re-runs a workflow until it passes**, or a bound trips. This is the primitive for **eventual consistency**.

`poll:` attaches to a **workflow** step only — never `send`/`validate`/`craft` (that's a `POLL_ON_NON_WORKFLOW_STEP` lint error). The workflow re-runs until its aggregate outcome is `success`:

```yaml
flow:
  # Fire the state change
  - type: send
    procedure: payments.send_settle_payment
    inputs: {}

  # Re-run this workflow (a get-status send + a validate-settled pair) until it passes.
  # payments.check_payment_settled is an ordinary workflow — see "Workflow Steps" above.
  - type: workflow
    procedure: payments.check_payment_settled
    poll:
      max_attempts: 10      # required — finite bound (integer ≥ 1)
      interval_ms: 1000     # optional base delay between attempts (default 0)
      backoff_ms: 500       # optional LINEAR increment added each attempt (default 0 → fixed interval)
      timeout_ms: 60000     # optional secondary wall-clock cap
```

**How the loop decides:**

- The polled workflow's own `validate` sub-step is the convergence condition. `success` → **stop early**; `failure` → **retry**; `error` / `exception` / `aborted` → **halt immediately** (a fault is never retried — retrying broken setup only buries the cause).
- **Exhaustion** (all `max_attempts` used, or `timeout_ms` elapsed with no pass) surfaces as the **last attempt's `failure`** — it re-enters the normal stop path, so `--continue-scenario-on-failure` still applies. No special exit code.
- The delay before attempt *n+1* is `interval_ms + (n−1) × backoff_ms`.
- Each attempt re-runs the workflow's sends, so the ledger gains indexed keys (`send_get_payment_status`, `send_get_payment_status[1]`, …). A **bare** `{{ ledger.<procedure>… }}` reference resolves to the **latest** attempt, so the workflow's validate always reads that attempt's fresh response.
- A polled workflow with **no validate sub-step** has nothing to converge on — lint warns (`POLL_WITHOUT_VALIDATE`).

Prefer `poll:` over a fixed `syz.wait` whenever you're waiting for the system to *reach a state*; reserve `syz.wait` for genuinely fixed windows (a rate-limit window, a TTL expiry).

---

## 5. Part 4: Execute — Run Your Tests

### What is "Execute"?

Executing means **running your scenarios** with the `syz run` command. SYZYGY processes each step, captures requests and responses, and produces detailed results.

### Lint First (Pre-Flight Check)

Before running, pre-flight your scenarios. `syz lint` validates schemas, resolves the environment, confirms every referenced procedure exists, and runs a residual scan for any unresolved `{{ os-env-var:... }}` expressions — all without making any HTTP calls.

```bash
syz lint --env local
```

**Additional flags:**
- `--plugin <name>` — Scope to specific plugin (required when multiple local plugins exist)
- `--suite <path>` — Lint only a scenario file, directory, or suite manifest (defaults to all of `.syz/scenarios/`)
- `--tag <label>` — Filter by tags
- `--example <name>` — Filter outline rows (for parameterised scenarios)
- `--json` — Structured output

### The `syz run` Command

**Basic Syntax:**

```bash
syz run --suite .syz/scenarios --env local
```

Both `--suite` and `--env` are **required** on every `syz run`. Pointing `--suite` at `.syz/scenarios` runs **all scenarios** under that directory and outputs results to `.syz/results/`.

### Command Flags Explained

| Flag | Purpose | Default | Example |
|---|---|---|---|
| `--suite` | **Required.** Path to a scenario file, a directory (runs every scenario under it), or a `.suite.yaml` manifest. This is the scenario selector. | Required | `--suite .syz/scenarios` or `--suite .syz/scenarios/e2e/payment_flow.yaml` |
| `--env` | **Required.** Environment to run against (selects `.syz/environments/<name>.yaml`). | Required | `--env local` |
| `--tag` | Narrow the selection by tag (comma-list, OR-union) | All in suite | `--tag smoke` |
| `--example` | Run specific outline examples (for parameterised scenarios) | All examples | `--example user-gold` |
| `--run-name` | Custom execution folder name | Auto-generated | `--run-name my-test-run` |
| `--live-stats` | Show live progress panel | `false` | `--live-stats` |
| `--reporter` | Output format (`junit` supported) | Default (JSON) | `--reporter junit` |
| `--output` | Custom output file path (only valid with `--reporter`) | `.syz/results/` | `--output ./results.xml` |
| `--continue-scenario-on-failure` | Don't stop on validate failures | `false` | `--continue-scenario-on-failure` |
| `--no-install` | Disable on-demand executor-driver provisioning | `false` | `--no-install` |
| `--yes` | Auto-accept driver-install consent prompts | `false` | `--yes` |

### Running Scenarios

**Run All Scenarios:**

```bash
syz run --suite .syz/scenarios --env local
```

Creates `.syz/results/run-2026-07-04T10-53-00-000Z/` with all results.

**Run Specific Scenario:**

```bash
syz run --suite .syz/scenarios/e2e/payment_flow.yaml --env local
```

**Run Multiple Scenarios:**

`--suite` takes a single path. To run several scenarios at once, point it at a directory — every scenario under it runs — or at a `.suite.yaml` manifest that lists them:

```bash
# Run every scenario under the e2e/ directory
syz run --suite .syz/scenarios/e2e --env local

# Or curate an explicit list in a suite manifest (see "Run a Suite" below)
syz run --suite .syz/suites/ci-critical.suite.yaml --env local
```

**Run by Tag:**

Scenarios can be tagged for categorization:

```yaml
# .syz/scenarios/e2e/payment_flow.yaml
name: "Payment Flow — Happy Path"
tags: [smoke, critical, payments]
...
```

Then run by tag:

```bash
syz run --suite .syz/scenarios --tag smoke --env local          # Run all tagged 'smoke'
syz run --suite .syz/scenarios --tag critical --env local       # Run all tagged 'critical'
syz run --suite .syz/scenarios --tag smoke,payments --env local # Run tagged 'smoke' OR 'payments'
```

**Run a Suite (Playlist):**

Create `.syz/suites/ci-critical.suite.yaml`:

```yaml
name: "CI Critical Path"
scenarios:
  - e2e/payment_flow.yaml
  - e2e/refund_workflow.yaml
  - integration/payment_status_check.yaml
```

Then run it by passing the manifest path to `--suite`:

```bash
syz run --suite .syz/suites/ci-critical.suite.yaml --env local
```

**Choose the Environment:**

```bash
# Run this scenario against the 'staging' environment
syz run --suite .syz/scenarios/e2e/payment_flow.yaml --env staging
```

The environment is chosen entirely by `--env` — it selects `.syz/environments/staging.yaml`, so the run uses that environment's base URL and credentials. Point `--env` at `local` to run the same scenario against your local environment instead.

**Custom Run Name:**

```bash
syz run --suite .syz/scenarios --env local --run-name deployment-verification
```

Results go to `.syz/results/deployment-verification/` instead of the default timestamped folder.

**Live Progress Panel:**

```bash
syz run --suite .syz/scenarios --env local --live-stats
```

Opens a native-looking progress panel (Chrome app mode) showing real-time status of each scenario.

### Handling Failures

**By Default (scenario execution stops on the first failure):**

```bash
syz run --suite .syz/scenarios --env local
# Scenario 1: ✓ pass
# Scenario 2: ✗ fail — run stops here
# Scenario 3: ✓ pass
# Exit code: 1
```

**Continue on Failure (scenario execution does not stop on the first failure):**

```bash
syz run --suite .syz/scenarios --env local --continue-scenario-on-failure
# Scenario 1: ✓ pass
# Scenario 2: ✗ fail — continue anyway
# Scenario 3: ✓ pass
# Exit code: 1 (still fails overall)
```

Use this when you want the scenario to continue to the end, instead of stopping at the first problem.

---

## 6. Part 5: Audit & Review — Read Your Results

### What is "Audit & Review"?

After running tests, you review the results to understand:

- **What passed and what failed?** (execution summary)
- **What was sent and what was received?** (request/response capture)
- **Which assertions passed and which failed?** (validation details)
- **How does this trace back to business requirements?** (traceability matrix)

### Generated Result Artifacts

Each `syz run` creates a timestamped folder: `.syz/results/run-2026-07-04T10-53-00-000Z/`

**Structure:**

```
.syz/results/run-2026-07-04T10-53-00-000Z/
├── execution.json              ← Execution-level summary
├── index.html                  ← Interactive debug viewer
├── junit.xml                   ← JUnit format (if --reporter junit)
└── e2e/
    └── payment_flow/
        ├── debug.log           ← Phase-by-phase trace (text)
        ├── ledger.json         ← Full request/response capture (JSON)
        └── crafted/            ← Generated artifacts (if any craft steps)
```

### Reading execution.json

**Location:** `.syz/results/<execution-id>/execution.json`

**Structure:**

```json
{
  "execution_id": "run-2026-07-04T10-53-00-000Z",
  "timestamp": "2026-07-04T10:53:00.000Z",
  "environment": "local",
  "suite_path": ".syz/scenarios",
  "total_scenarios": 2,
  "passed": 1,
  "failed": 1,
  "total_duration_ms": 3334,
  "continue_scenario_on_failure": false,
  "scenarios": [
    {
      "scenario_id": "e2e/payment_flow",
      "name": "Payment Flow — Happy Path",
      "level": "e2e",
      "status": "success",
      "duration_ms": 1234,
      "results_path": "e2e/payment_flow",
      "steps": [
        { "step_number": 1, "procedure": "payments.send_create_payment", "type": "send", "outcome": "success" },
        { "step_number": 2, "procedure": "payments.validate_payment_created", "type": "validate", "outcome": "success" }
      ]
    },
    {
      "scenario_id": "e2e/payment_refund",
      "name": "Payment Refund — Happy Path",
      "level": "e2e",
      "status": "failure",
      "duration_ms": 2100,
      "results_path": "e2e/payment_refund",
      "steps": [
        { "step_number": 1, "procedure": "payments.send_create_payment", "type": "send", "outcome": "success" },
        { "step_number": 2, "procedure": "payments.send_refund_payment", "type": "send", "outcome": "success" },
        { "step_number": 3, "procedure": "payments.validate_refund_applied", "type": "validate", "outcome": "failure" }
      ]
    }
  ]
}
```

*(Outline scenarios add `outline_name` / `example_name` per row; a filtered run adds a `selection` block recording the suite, tags, and examples that were matched or skipped.)*

**What Each Field Means:**

- **`status`** (per scenario) and **`outcome`** (per step): `success`, `failure`, `error`, `exception`, `aborted`
  - `success`: All steps passed
  - `failure`: ≥1 validation failed (test logic mismatch)
  - `error`: ≥1 step couldn't run (bad reference, wrong syntax)
  - `exception`: External system down (API unreachable, timeout)
  - `aborted`: SYZYGY internal bug

- **`steps`**: Each step's procedure, type, and outcome in sequence

- **`passed` / `failed`**: Scenario counts for the whole run

### Reading index.html

**Location:** `.syz/results/<execution-id>/index.html`

The **interactive debug viewer**. Open in a browser (double-click the file or `open index.html`).

**Sections:**

1. **Header + Execution Summary** — Run ID, environment badge, timestamp, and total duration in the header (plus a `continue-on-failure` badge and doc-skew / upgrade-available banners when applicable). Below it, four stat cards: Total, Passed, Failed, Duration.
2. **Controls** — Expand All / Collapse All, a status filter (All / Success / Failure / Error / Exception / Aborted, each with a count), and a scenario search box.
3. **Scenario List** — One expandable card per scenario, showing its status, name, and duration, plus a copy-trace button. Each card ends with direct links to its `debug.log` and `ledger.json` — the deeper detail lives there, one click away.
4. **Step Details** (inside each scenario) — Only the steps that sent a request or checked a response appear here (a craft step such as `syz.wait` or `syz.set_context_var`, which has neither, is omitted — its detail is in `debug.log`; a failed one stays visible so you always see where a scenario broke). One expandable block per shown step, with status, type, procedure, and duration. Expanding a step shows just the two blocks you reason about, inline (no second expand):
   - **Executor** — the request/response exchange: a `METHOD url` (or status) line, a header table, and a pretty-printed body. The request offers **Copy JSON** and, for an HTTP request, **Copy as cURL**; the response shows its status, reason phrase, and size/format/duration.
   - **Assertions** — each check, pass/fail, with rule, expected, actual, and detail.

   The resolution phases behind these — input resolution, payload construction, context variables, and the ledger write — are recorded in `debug.log` (and `ledger.json`), reachable from the card's links, rather than shown inline. A step's error message renders inline within its block. A **workflow** step is shown as a tree — one row named after the workflow, with its steps nested beneath it (so it matches your scenario file); a polled workflow nests each attempt the same way. A workflow whose sub-steps are all omitted (no request or checks) is dropped entirely.

**Color Coding:**

- 🟢 **Green**: Passed
- 🔴 **Red**: Failed (assertion mismatch)
- 🟠 **Orange**: Error (couldn't execute)
- 🔵 **Blue**: Info / In Progress

### Reading debug.log

**Location:** `.syz/results/<execution-id>/<scenario>/debug.log`

A **plain-text transcript** of the entire scenario execution. Useful for CI logs or quick text search.

**Example:**

```
Scenario: Create then settle a payment
ID:       e2e/payment_flow
Status:   SUCCESS
Duration: 257ms
Started:  2026-07-04T10:53:00.000Z

─── STEP 1: send — payments.send_create_payment ────────────────────────

  [Input Resolution]
    amount            5000  (concrete)
    currency          "GBP"  (concrete)
    customer_id       "cust_alice"  (concrete)

  [Payload Construction]
    Rendered: {"request":{"fields":{"base_url":"https://api.payments.example.com/v1","method":"POST","path":"/payments","body":{"type":"raw","content":{"amount":5000,"currency":"GBP","customer_id":"cust_alice"}}},"headers":{"Authorization":"[MASKED]"}},"settings":{}}
    Connection:
      request.fields.base_url = "https://api.payments.example.com/v1"  [procedure]
      request.fields.body.content.amount = 5000  [procedure]
      request.fields.body.content.currency = "GBP"  [procedure]
      request.fields.body.content.customer_id = "cust_alice"  [procedure]
      request.fields.body.type = "raw"  [procedure]
      request.fields.method = "POST"  [procedure]
      request.fields.path = "/payments"  [procedure]
      request.headers.Authorization = via {{ os-env-var:PAYMENTS_TOKEN }}  [MASKED]  [env]

  [Executor]
    →  POST https://api.payments.example.com/v1/payments
       {"Authorization":"[MASKED]"}
       {"amount":5000,"currency":"GBP","customer_id":"cust_alice"}
    ←  201  245ms
       {"id":"pay_abc123xyz","status":"pending","amount":5000,"currency":"GBP"}

  [Ledger Write]
    Key:       payments.send_create_payment
    Response:  http_status=201  size=88B  format=json
    Outputs:   {"id":"pay_abc123xyz"}
    Timestamp: 2026-07-04T10:53:00.245Z
    Duration:  245ms

✓ SUCCESS  245ms

─── STEP 2: validate — payments.validate_payment_created ───────────────

  [Input Resolution]
    response          "{{ ledger.payments.send_create_payment.http.response }}"  →  {"fields":{"status_code":201,"reason_phrase":"Created","http_version":"1.1","body":{"id":"pay_abc123xyz","status":"pending","amount":5000,"currency":"GBP"}},"headers":{"content-type":"application/json; charset=utf-8"},"format":"json","size_bytes":88}

  [Assertions]
    ✓ Response status is 201   [http · equals · $.fields.status_code]  →  201
    ✓ Payment ID is present   [json · exists · $.fields.body.id]
    ✓ Status is pending or processing   [json · in · $.fields.body.status]  →  ["pending","processing"]

  [Ledger Write]
    Key:       payments.validate_payment_created
    Timestamp: 2026-07-04T10:53:00.257Z
    Duration:  2ms

✓ SUCCESS  12ms
```

Each step prints up to six bracketed phase sections — `[Input Resolution]`, `[Payload Construction]`, `[Executor]`, `[Assertions]`, `[Context Variables]`, `[Ledger Write]` — followed by its outcome (`✓ SUCCESS`, `✕ FAILURE`, `! ERROR`, `! EXCEPTION`, `‼ ABORTED`).

### Reading ledger.json

**Location:** `.syz/results/<execution-id>/<scenario>/ledger.json`

The **authoritative machine-readable log** of the scenario execution. Every request, response, and assertion result is recorded here.

**Structure:**

The file is an object **keyed by procedure** (a repeated procedure gets indexed keys: `procedure`, `procedure[1]`, `procedure[2]`, …). A `send` entry nests its request/response under the executor key (`http:`); the HTTP response envelope carries `fields` (status code, body) and `headers`:

```json
{
  "payments.send_create_payment": {
    "procedure": "payments.send_create_payment",
    "type": "send",
    "http": {
      "request": {
        "fields": {
          "base_url": "https://api.payments.example.com/v1",
          "method": "POST",
          "path": "/payments",
          "body": {
            "type": "raw",
            "content": {
              "amount": 5000,
              "currency": "GBP",
              "customer_id": "cust_alice"
            }
          }
        },
        "headers": {
          "Authorization": "[MASKED]"
        }
      },
      "response": {
        "fields": {
          "status_code": 201,
          "body": {
            "id": "pay_abc123xyz",
            "status": "pending",
            "amount": 5000,
            "currency": "GBP"
          }
        },
        "headers": {
          "content-type": "application/json"
        },
        "format": "json",
        "size_bytes": 145
      }
    },
    "outputs": {
      "id": "pay_abc123xyz",
      "status": "pending"
    },
    "outcome": "success",
    "timestamp": "2026-07-04T10:53:00.000Z",
    "duration_ms": 245
  },
  "payments.validate_payment_created": {
    "procedure": "payments.validate_payment_created",
    "type": "validate",
    "outcome": "success",
    "validation": {
      "checks": [
        {
          "description": "Response status is 201",
          "type": "http",
          "path": "$.fields.status_code",
          "operator": "equals",
          "passed": true,
          "expected": 201,
          "actual": 201
        },
        {
          "description": "Payment ID is present",
          "type": "json",
          "path": "$.fields.body.id",
          "operator": "exists",
          "passed": true
        }
      ]
    },
    "timestamp": "2026-07-04T10:53:00.000Z",
    "duration_ms": 12
  }
}
```

These entries are exactly what `{{ ledger.… }}` references resolve against: `{{ ledger.payments.send_create_payment.http.response.fields.body.id }}` walks this structure, and `{{ ledger.payments.send_create_payment.outputs.id }}` is the declared-output shortcut.

### Debugging Failed Tests

**Scenario: Assertion Failed**

You run a test and it fails. Let's diagnose:

```bash
# 1. Check the execution summary
cat .syz/results/run-2026-07-04T10-53-00-000Z/execution.json | grep -B2 -A1 "failure"

# Output shows:
# "status": "failure",
# ...
# { "step_number": 3, "procedure": "payments.validate_payment_amount", "type": "validate", "outcome": "failure" }
```

**2. Open index.html in browser**

Navigate to Step 3, look at the "Assertions" section:

```
Check: "Amount matches request"
Expected: 5000
Actual: 5001
✗ FAILED
```

**3. Check the debug.log for context**

```bash
cat .syz/results/run-2026-07-04T10-53-00-000Z/e2e/payment_flow/debug.log | grep -A20 "validate_payment_amount"
```

You see the actual response body. The API returned `5001` (cents = $50.01) instead of `5000`. The issue is **in the test data**, not the code.

**4. Fix and Re-run**

Update `.syz/scenarios/e2e/payment_flow.yaml`:

```yaml
inputs:
  amount: 5001  # or fix the test expectation
```

Then:

```bash
syz run --suite .syz/scenarios/e2e/payment_flow.yaml --env local
```

### Requirements Traceability Matrix (RTM)

The `syz rtm` command maps every requirement in your knowledge files — `DOMAIN.yaml` and each plugin's `PLUGIN-GUIDE.yaml` — to the tests that cover it. Coverage is declared on the test side with `covers:`, on a scenario, an outline example row, or an individual step:

```yaml
name: "Payment Flow — Happy Path"
covers: [REQ-PAYMENT-DOM-001]
```

**Basic Syntax:**

```bash
syz rtm            # writes .syz/rtm.md — the human-readable matrix
```

**With Options:**

```bash
syz rtm --json     # writes .syz/rtm.json instead — machine-readable
syz rtm --strict   # exit 1 if any requirement is uncovered or a covers: dangles (the CI gate)
syz rtm --lastrun  # annotate each entry with its outcome from the latest run
```

**Terminal Output:**

```
  ✓ wrote .syz/rtm.md
  ⚠ 1 uncovered requirement
  ✓ 0 dangling covers: references
  • 1 in-progress requirement excluded (ids beginning with '_')
```

**The generated `.syz/rtm.md`:**

```markdown
# Requirements Traceability Matrix (RTM)

> Derived report — generated by `syz rtm`. Do not hand-edit; set `covers:` on your tests and regenerate.
>
> 1 in-progress requirement excluded (ids beginning with `_`). Drop the leading `_` to track coverage.

| Requirement | Covered by |
|---|---|
| REQ-PAYMENT-DOM-001 | e2e/payment_happy_path; e2e/payment_refund |
| REQ-PAYMENTS-PG-001 | component/validate_currencies ▸ row:invalid_currency |
| REQ-REFUND-DOM-001 | integration/refund_workflow ▸ step:2 (payments.refund) |

## Uncovered (1)
- REQ-PAYMENTS-PG-003

## Dangling covers: (0)
```

The RTM is a **derived report**, like the run report — never maintained by hand. With `--json`, `.syz/rtm.json` carries the same model plus a summary (`total`, `covered`, `uncovered`, `orphans`, `in_progress_excluded`) and a `gaps` list for scripting.

**In-progress requirements:** while the work behind a requirement is still in flight, prefix its id with an underscore (`_REQ-PAYMENT-DOM-002`, as in the DOMAIN.yaml example above). It stays fully validated, but it's left out of the coverage floor — `syz rtm` won't list it as a gap, `--strict` won't fail on it, and the summary notes how many were excluded. Drop the leading `_` when it's ready to track: it's the same requirement, so any `covers:` already pointing at it keeps working (`syz lint` warns while a test covers a requirement that's still in progress).

Use this to identify **test gaps** — which requirements need more scenario coverage?

---

## 7. Running Example: Complete Walkthrough

Now let's put it all together with a complete, step-by-step example.

### Scenario: The Payments API Integration

**Goal:** Set up a plugin for a Payments API, write test scenarios, run them, and review results.

### Step 1: Initialise the Workspace

```bash
# Create a new directory
mkdir payments-integration
cd payments-integration

# Initialise SYZYGY
syz init

# This creates:
# .syz/
# ├── dossier/
# │   ├── DOMAIN.yaml (scaffolded)
# │   └── TEST-STRATEGY.md (scaffolded)
# ├── plugins/ (empty)
# ├── scenarios/ (empty)
# ├── environments/ (empty)
# └── CLAUDE.md
```

### Step 2: Define Your Domain (DOMAIN.yaml)

Edit `.syz/dossier/DOMAIN.yaml`:

```yaml
summary: >
  Payments processing for e-commerce orders.

areas:
  - key: PAYMENT
    title: Payment processing

requirements:
  - id: REQ-PAYMENT-DOM-001
    behaviour:
      given: an order with a valid payment method
      when: the customer pays
      then: the payment settles and its status can be tracked
    rules:
      - payments must be positive amounts (in minor units — cents)
      - only AUD, CNY, EUR, GBP, INR, JPY, and USD are accepted
      - a payment's status is one of pending, completed, failed, refunded
    notes: >
      The core journey to test: create a payment and verify its status, run
      the refund workflow, and reject unsupported currencies.
```

### Step 3: Create OpenAPI Spec

Create `payments-api.openapi.yaml`:

```yaml
openapi: 3.0.0
info:
  title: Payments API
  version: 1.0.0
servers:
  - url: https://api.payments.example.com/v1

paths:
  /payments:
    post:
      operationId: createPayment
      summary: Create a payment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount, currency, customer_id]
              properties:
                amount:
                  type: integer
                  description: Amount in cents
                  example: 5000
                currency:
                  type: string
                  example: GBP
                customer_id:
                  type: string
                  example: cust_123
      responses:
        '201':
          description: Payment created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  status:
                    type: string
                  amount:
                    type: integer
```

### Step 4: Generate Plugin

```bash
syz generate plugin --name payments --from openapi:./payments-api.openapi.yaml
```

This creates `.syz/plugins/payments/` with send/validate/workflow procedures and `data.yaml`, writes sample scenarios under `.syz/scenarios/generated/payments/`, and seeds `.syz/environments/local.yaml` with each step's `base_url` and `auth_token`.

### Step 5: Set Up Environment

`syz generate` already seeded `.syz/environments/local.yaml` — review it and point it at your staging host:

```yaml
name: local
production: false

payments:
  send_create_payment:
    base_url: "https://api-staging.payments.example.com/v1"
    auth_token: "{{ os-env-var:PAYMENTS_AUTH_TOKEN }}"
  send_get_payment_status:
    base_url: "https://api-staging.payments.example.com/v1"
    auth_token: "{{ os-env-var:PAYMENTS_AUTH_TOKEN }}"
```

Create `.syz/environments/production.yaml` with the same shape, the production host, and `production: true`.

Set the environment variable the seeded file references — one per plugin, covering every operation in it:

```bash
export PAYMENTS_AUTH_TOKEN="sk_live_abc123xyz"
```

### Step 5b: Adapt the Generated Plugin (add validate checks)

The generator gives you runnable procedures — and the send procedure already drives its request body from each scenario's `inputs:`, so there's nothing to wire by hand. The one thing left to author is the richer validate procedures your scenarios call. This step is plugin-side authoring — see [§9 Appendix](#9-appendix-artefact-reference--author-anything-by-hand) for the full schema of every field used here.

**1. The send procedure is already wired to your scenario inputs.** `syz generate` emits `send_create_payment` with a `defaults:` block — one entry per body field, each seeded from `data.yaml` — and a request body whose `content` reads those values back through `{{ inputs.* }}`. So a scenario runs with `inputs: {}` and posts the `data.yaml` sample, or sets `inputs: { amount: 500 }` to override just that field while the rest stay sampled. No editing required; this is the shape the generator produces:

`.syz/plugins/payments/procedures/send_create_payment.yaml`:

```yaml
name: payments.send_create_payment
type: send
execution_library: http
description: "Create a new payment"
outputs:
  id:
    path: "$.fields.body.id"
    description: "ID of the created payment"
defaults:                              # each body field seeded from data.yaml…
  amount: "{{ plugin.payments.data.create_payment.body.amount }}"
  currency: "{{ plugin.payments.data.create_payment.body.currency }}"
  customer_id: "{{ plugin.payments.data.create_payment.body.customer_id }}"
http:
  request:
    fields:
      base_url: "{{ env.payments.send_create_payment.base_url }}"
      method: POST
      path: /payments
      body:
        type: raw
        content:                       # …and read back here through the merged inputs
          amount: "{{ inputs.amount }}"
          currency: "{{ inputs.currency }}"
          customer_id: "{{ inputs.customer_id }}"
    headers:
      Authorization: "Bearer {{ env.payments.send_create_payment.auth_token }}"
sensitive_fields:
  - http.request.headers.Authorization
```

A scenario overrides only what it names: `inputs: {}` sends all three fields from `data.yaml`; `inputs: { amount: 9999 }` sends `amount: 9999` with `currency`/`customer_id` still sampled. (This per-field wiring is emitted for JSON request bodies; file-upload and multipart sends stream a file instead and expose no body inputs.)

**2. Author the two validate procedures the scenarios call.** The generator emits a status-only `validate_create_payment`; the happy-path scenario wants richer checks under its own names. Create these two files under `.syz/plugins/payments/procedures/`:

`validate_payment_created.yaml`:

```yaml
name: payments.validate_payment_created
type: validate
description: "A payment was created — 201 with a well-formed body"
checks:
  - type: http
    description: "HTTP status matches expected"
    path: "$.fields.status_code"
    operator: equals
    value_from_input: expected_status
  - type: json
    description: "Payment ID is present"
    path: "$.fields.body.id"
    operator: exists
  - type: json
    description: "Status is a known lifecycle state"
    path: "$.fields.body.status"
    operator: in
    value: ["pending", "processing", "completed"]
```

`validate_payment_status.yaml`:

```yaml
name: payments.validate_payment_status
type: validate
description: "The payment reached the expected lifecycle status"
checks:
  - type: json
    description: "Body status matches expected"
    path: "$.fields.body.status"
    operator: equals
    value_from_input: expected_status
```

`expected_status` means two different things by design: on `validate_payment_created` it's an **HTTP status code** (`201`, checked against `$.fields.status_code`), and on `validate_payment_status` it's a **body value** (`"completed"`, checked against `$.fields.body.status`). Each validate procedure decides what its inputs assert against.

### Step 6: Write Test Scenarios

Create `.syz/scenarios/e2e/payment_happy_path.yaml`:

```yaml
name: "Payment Flow — Happy Path"
level: e2e
plugins:
  - name: payments
    version: "1.0.0"

flow:
  # Create a payment
  - type: send
    procedure: payments.send_create_payment
    inputs:
      amount: 5000
      currency: "GBP"
      customer_id: "cust_alice"

  # Verify it was created
  - type: validate
    procedure: payments.validate_payment_created
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
      expected_status: 201

  # Get the payment status
  - type: send
    procedure: payments.send_get_payment_status
    inputs:
      id: "{{ ledger.payments.send_create_payment.outputs.id }}"

  # Verify status is completed
  - type: validate
    procedure: payments.validate_payment_status
    inputs:
      response: "{{ ledger.payments.send_get_payment_status.http.response }}"
      expected_status: "completed"
```

Create `.syz/scenarios/e2e/payment_parameterized.yaml` (test multiple currencies):

```yaml
name: "Payments — Multiple Currencies"
level: e2e
plugins:
  - name: payments
    version: "1.0.0"

flow:
  - type: send
    procedure: payments.send_create_payment
    inputs:
      amount: 5000
      currency: "{{ outline.currency }}"
      customer_id: "cust_alice"

  - type: validate
    procedure: payments.validate_payment_created
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
      expected_status: "{{ outline.expected_status }}"

examples:
  - name: gbp-success
    currency: "GBP"
    expected_status: 201

  - name: usd-success
    currency: "USD"
    expected_status: 201

  - name: eur-success
    currency: "EUR"
    expected_status: 201

  - name: invalid-xxx
    currency: "XXX"
    expected_status: 422
```

### Step 7: Run Tests

```bash
# Run all scenarios
syz run --suite .syz/scenarios --env local

# Or run specific scenario
syz run --suite .syz/scenarios/e2e/payment_happy_path.yaml --env local

# Or run with live progress
syz run --suite .syz/scenarios --env local --live-stats

# Or run specific parameterised example
syz run --suite .syz/scenarios/e2e/payment_parameterized.yaml --env local
```

### Step 8: Review Results

```bash
# Check execution summary
cat .syz/results/run-2026-07-04T10-53-00-000Z/execution.json

# Open interactive viewer
open .syz/results/run-2026-07-04T10-53-00-000Z/index.html

# Check plain-text log
cat .syz/results/run-2026-07-04T10-53-00-000Z/e2e/payment_happy_path/debug.log

# Check full ledger
cat .syz/results/run-2026-07-04T10-53-00-000Z/e2e/payment_happy_path/ledger.json | jq
```

### Step 9: Create Requirements Traceability Matrix

```bash
syz rtm

# Writes .syz/rtm.md — which requirements from DOMAIN.yaml / PLUGIN-GUIDE.yaml
# are covered by your tests, and which are still uncovered
```

### Step 10: Deploy to CI/CD

Add to your CI pipeline:

```bash
#!/bin/bash
set -e

# Install dependencies
npm install -g syzs-cli

# Run tests
syz run --suite .syz/scenarios --env staging --reporter junit --output ./test-results.xml

# Coverage gate: exit 1 if any requirement is uncovered or a covers: dangles
syz rtm --strict

# Optional: machine-readable matrix for your CI dashboard (writes .syz/rtm.json)
syz rtm --json
```

---

## 8. Common Workflows & Troubleshooting

### Workflow 1: "I Found a Bug in My API"

**Scenario:** A test fails because the API returned an unexpected error.

**Diagnosis:**

1. Open `index.html` from the failed run
2. Find the failed step
3. Look at the **Executor** block — what was the request sent and the actual response?
4. Look at the **Assertions** block — which check failed, and what was expected vs actual?
5. For the inputs and constructed payload behind the request, open the step's `debug.log` (linked from the scenario card) and read its **Input Resolution** and **Payload Construction** sections.

**Solution:**

Usually one of:
- **API bug:** Report to dev team with ledger.json attached (proves what was sent)
- **Test bug:** Update scenario inputs or assertions
- **Integration bug:** Environment/credentials issue

### Workflow 2: "I Want to Verify My Tests Are Actually Testing"

**Scenario:** You want proof that your test would catch a real bug.

**Verification:**

1. Temporarily break the assertion:

```yaml
checks:
  - type: json
    path: "$.fields.body.id"
    operator: equals
    value: "wrong-id-on-purpose"
```

2. Run: `syz run --suite .syz/scenarios/e2e/your-scenario.yaml --env local`
3. Verify it **fails** (if it passes, your test isn't actually checking that field)
4. Revert the change

This is **mutation testing** — ensuring your assertions catch real changes.

### Workflow 3: "I Need to Test Against Multiple Environments"

**Scenario:** Same tests, different servers (local, staging, production).

**Solution:**

```bash
# Local
syz run --suite .syz/scenarios --env local

# Staging
syz run --suite .syz/scenarios --env staging

# Production (read-only, no destructive tests)
syz run --suite .syz/scenarios --env production --tag smoke
```

Each environment has its own `.syz/environments/<name>.yaml` with different base URLs and credentials.

### Workflow 4: "My Test Passes Locally but Fails in CI"

**Scenario:** Flaky or environment-dependent test.

**Diagnosis:**

1. Fetch the CI run folder (`.syz/results/<run-id>/`) as a build artifact
2. Compare ledger.json from CI vs local
3. Look for differences in:
   - Response timestamps
   - Base URLs (is CI using the right environment?)
   - Credentials (are env vars set?)
   - Rate limiting (did CI hit a rate limit?)

**Common Causes:**

- Credentials not exported in CI
- Wrong environment flag (`--env staging` missing?)
- Data cleanup between runs (CI doesn't clean up, next run conflicts with previous)
- Timing issues (use `syz.wait` craft step to add delays)

### Workflow 5: "I Need to Mask Sensitive Data"

**Scenario:** Logs contain API keys that shouldn't be visible.

**Solution:**

Mark sensitive fields in the procedure:

```yaml
name: payments.send_create_payment
type: send
sensitive_fields:
  - http.request.headers.Authorization
  - http.response.fields.body.access_token
```

SYZYGY automatically masks these in:
- `debug.log` (shows `[MASKED]`)
- `index.html` (shows `[MASKED]`)
- `ledger.json` (shows `[MASKED]`)
- Terminal output

The **actual value is preserved internally** and forwarded to later steps (so workflows still work), but hidden from logs.

### Workflow 6: "I Need to Generate a Report for Management"

**Scenario:** Convert test results to a format for stakeholders.

**Solution:**

```bash
# Generate JUnit XML for CI tools (Jenkins, GitLab, GitHub)
syz run --suite .syz/scenarios --env local --reporter junit --output ./results.xml

# Convert to HTML report
syz run --suite .syz/scenarios --env local

# Then use the generated index.html (share with team)

# Generate RTM for coverage reporting (writes .syz/rtm.json)
syz rtm --json

# Embed in your CI dashboard
```

---

## 9. Appendix: Artefact Reference — Author Anything by Hand

Parts 1–7 lean on `syz generate` and your AI assistant. This appendix is the counterpart: the **complete authored shape of every `.syz` artefact**, so you can create or edit any of them by hand with nothing but this page. Each field below is the real schema `syz lint` enforces.

Everything a plugin ships lives under `.syz/plugins/<name>/`:

```
.syz/plugins/payments/
├── plugin.yaml            ← identity (name + version)
├── PLUGIN-GUIDE.yaml      ← knowledge layer (see §2)
├── procedures/            ← send / validate / workflow / craft — one .yaml each
├── models/               ← optional input models (referenced by input_model:)
├── templates/            ← optional Nunjucks .njk files (craft procedures only)
└── data.yaml             ← optional shared constants + example bodies
```

### 9.1 `plugin.yaml` — plugin identity

The whole file is two fields. The **folder name is the plugin name** and must match `plugin:`; the version is a plain string.

```yaml
plugin: payments
version: 1.0.0
```

There is no name or version anywhere else — `PLUGIN-GUIDE.yaml`, procedures, and scenarios never repeat them.

### 9.2 Send procedure — `procedures/<name>.yaml`

A send procedure defines one HTTP request as a **structured block** (no template). Full field set:

| Field | Required | Holds |
|---|---|---|
| `name` | ✓ | `<plugin>.<procedure>` — must be prefixed with the plugin folder name |
| `type` | ✓ | `send` |
| `execution_library` | ✓ (send) | `http` — the executor. HTTP is the only one shipped today |
| `description` | | What this operation does — be precise and thorough. AI scenario-authoring tools read this to understand the procedure's purpose and select the right one when building flows. |
| `outputs` | | Named shortcuts to extract response fields. Each key becomes `{{ ledger.<plugin>.<proc>.outputs.<key> }}`; declared as `<key>: { path: "<JSONPath into response envelope>", description: "<what this value represents>" }` |
| `defaults` | | Default input values; step `inputs:` merge **over** these |
| `sensitive_fields` | | Dotted paths masked in every artifact (e.g. `http.request.headers.Authorization`) |
| `connection` | | An inline connection handle (base identity), merged **under** the `http:` block. Optional for HTTP — see §9.9 |
| `http` | ✓ (send) | The request: `{ request: { fields, headers }, settings }` |

**The `http` block in full:**

```yaml
name: payments.send_create_payment
type: send
execution_library: http
description: "Create a new payment"
outputs:
  id:     { path: "$.fields.body.id",     description: "ID of the created payment" }
  status: { path: "$.fields.body.status", description: "Current status" }
http:
  request:
    fields:
      method: POST                        # GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS
      base_url: "{{ env.payments.send_create_payment.base_url }}"   # location — OR a full url:
      path: /payments                     # joined to base_url with one slash
      query: { limit: 50, ids: [1, 2] }   # arrays → repeat-key (?ids=1&ids=2) by default
      body:
        type: raw                         # none | raw | form-data | x-www-form-urlencoded | binary
        content:
          amount: "{{ inputs.amount }}"
          currency: "{{ inputs.currency }}"
          customer_id: "{{ inputs.customer_id }}"
    headers:                              # OPEN namespace — any valid header name
      Authorization: "Bearer {{ env.payments.send_create_payment.auth_token }}"
      Accept: "application/json"
  settings:                               # transport tuning (closed set — all optional)
    timeout_ms: 30000                     # default 30000
    follow_redirects: true                # default true
    max_redirects: 5                      # default 5
    query_array_format: repeat            # repeat | bracket | comma
    max_capture_bytes: 5242880            # response-body capture cap (~5 MB default)
    tls:   { verify: true, ca_cert: "./ca.pem", client_cert: "./c.pem", client_key: "./k.pem", client_key_passphrase: "{{ os-env-var:KEY_PASS }}" }
    proxy: { url: "http://corp-proxy:8080", username: "{{ os-env-var:PROXY_USER }}", password: "{{ os-env-var:PROXY_PASS }}" }
sensitive_fields:
  - http.request.headers.Authorization
```

**URL — two mutually-exclusive modes.** `base_url` + `path`, **or** a single full `url:`. Setting both is a lint error (`URL_MODE_CONFLICT`); setting neither is `URL_MISSING`.

**Body — `body.type` decides `content`'s shape:**

| `body.type` | `content` is | default Content-Type |
|---|---|---|
| `none` | absent | — |
| `raw` | object (→JSON), string (sent verbatim), or one `{ file }` | object→`application/json`, else `text/plain` |
| `form-data` | key → text or `{ file: "<path>", filename?, content_type? }` | `multipart/form-data` |
| `x-www-form-urlencoded` | flat key → text map (no files) | `application/x-www-form-urlencoded` |
| `binary` | exactly one `{ file }` | file's `content_type` or `application/octet-stream` |

A file is **always** `{ file: "<path>" }`, never a bare path. A *dynamic* body (loops/conditionals) is built by a craft step (§9.5) and referenced as `content: "{{ ledger.<plugin>.<craft>.response.body }}"`.

**A send step never judges itself.** Transport/protocol errors are captured as response *data* (`body.error`), not thrown — pair every send with a validate (or use a `<op>_send_success` workflow) to turn a clean send into a pass.

### 9.3 Validate procedure — `procedures/<name>.yaml`

A validate procedure is a list of `checks:` run against a response passed in as an input. Field set:

| Field | Required | Holds |
|---|---|---|
| `name` / `type` | ✓ | `<plugin>.<procedure>` / `validate` |
| `description` | | One line |
| `input_model` | | Path to a `models/<name>.yaml` declaring the inputs (§9.7) |
| `checks` | ✓ | The list of assertions |

Each **check** has a `type` (`http` \| `json` \| `text`), a `description`, an `operator`, and a target:

- `path` — a JSONPath into the response envelope (`$.fields.status_code`, `$.fields.body.*`, `$.headers.*`). Used by `json` and `http`.
- `content` — an input key holding the text to test (or omit for the whole body). Used by `text`.
- The expected value: a literal `value:`, or `value_from_input: <key>` to pull it from a step input.

```yaml
name: payments.validate_payment_created
type: validate
description: "Payment created — 201, well-formed body, known currency"
input_model: models/validate_payment_created_input.yaml
checks:
  - type: http                                   # equality / numeric on a JSONPath
    description: "HTTP status matches expected"
    path: "$.fields.status_code"
    operator: equals
    value_from_input: expected_status
  - type: json                                   # string ops + JSONPath into the body/headers
    description: "Payment ID is present"
    path: "$.fields.body.id"
    operator: exists
  - type: json
    description: "Currency is supported"
    path: "$.fields.body.currency"
    operator: in
    value: ["AUD", "CNY", "EUR", "GBP", "INR", "JPY", "USD"]
  - type: json                                   # nested all/any carry sub-checks
    description: "Every line item has a positive amount"
    path: "$.fields.body.items[*].amount"
    operator: all
    checks:
      - { type: json, operator: greater_than, value: 0 }
  - type: json                                   # sandboxed JS over the whole response
    description: "Total equals subtotal plus tax"
    operator: expression
    expression: "response.fields.body.total === response.fields.body.subtotal + response.fields.body.tax"
  - type: json                                   # validate structure against JSON Schema
    description: "Body matches the payment schema"
    path: "$.fields.body"
    operator: matches_schema
    value:
      type: object
      required: [id, amount, currency, status]
      properties:
        id:       { type: string }
        amount:   { type: number }
        currency: { type: string }
        status:   { type: string }
```

The full operator menu is the table in [§4 → Assertion Operators](#type-2-validate-steps). Notes on picking a `type`: use **`http`** for status-code equality and numeric comparisons; use **`json`** for anything on the body or headers, including all string operators (`contains`, `matches`) — it JSONPaths the whole envelope, so `$.headers.content-type` is reachable. `operator: ai` on any type sends the JSONPath'd value to the AI with a natural-language `instruction:` (needs `.syz/ai-api.yaml` configured).

### 9.4 Workflow procedure — `procedures/<name>.yaml`

A reusable ordered list of steps. Each step names a `procedure:` and supplies its `inputs:` inline. A scenario invokes the whole workflow as one step and customises children with `overrides:` (see [§4 → Workflow Steps](#type-3-workflow-steps)).

```yaml
name: payments.create_payment_send_success
type: workflow
description: "createPayment happy path — send, then confirm no transport error"
steps:
  - procedure: payments.send_create_payment
    inputs:
      amount: 5000
      currency: "GBP"
      customer_id: "cust_alice"
  - procedure: payments.validate_send_no_error   # shared no-error validate, one per plugin
    inputs:
      response: "{{ ledger.payments.send_create_payment.http.response }}"
```

A workflow **leaf** step may also carry `set:` (write `ctx.var.*`), `connection:` (name a handle), and `sensitive_fields:` — exactly like a top-level scenario step.

### 9.5 Craft procedure — `procedures/<name>.yaml`

A craft procedure produces a local artefact (XML, JSON, CSV, …) with **no network call**. The content lands in the ledger and, optionally, on disk. The `crafter:` field picks the strategy — **`nunjucks` (default) or `ai`**.

| Field | Required | Holds |
|---|---|---|
| `name` / `type` | ✓ | `<plugin>.<procedure>` / `craft` |
| `crafter` | | `nunjucks` (default) or `ai` |
| `template` | | Path to a `templates/<name>.njk` (nunjucks) — or a raw reference file (ai) |
| `instruction` | ✓ for `ai` | The AI prompt; supports `{{ inputName }}` expressions |
| `output_to_file` | | Filename to write under `results/<run>/<scenario>/crafted/<proc>/` — supports `{{ }}`; absent = in-memory only |
| `outputs` / `defaults` | | As for other procedures. `execution_library` is **not** set — craft has no executor |

Nunjucks crafter:

```yaml
name: accounting.craft_invoice_xml
type: craft
crafter: nunjucks              # optional — the default when absent
description: "Render an invoice XML from inputs"
outputs:
  filePath: { path: "$.metadata.output_file_path", description: "Path to the written file" }
output_to_file: "invoice_{{ inputs.payment_id }}.xml"
template: templates/invoice.xml.njk
```

AI crafter (`instruction` required, called at `temperature: 0` for determinism):

```yaml
name: accounting.craft_invoice_xml
type: craft
crafter: ai
instruction: "Generate a valid XML invoice for payment {{ payment_id }}, amount {{ amount }} {{ currency }}"
template: templates/invoice_schema.xsd    # optional — sent raw as a structural reference
output_to_file: "invoice.xml"
```

The rendered content is always at `{{ ledger.<plugin>.<craft>.response.body }}`; the written path (when `output_to_file` is set) is at `{{ ledger.<plugin>.<craft>.outputs.filePath }}` via the declared output.

### 9.6 Nunjucks template — `templates/<name>.njk`

Used **only** by craft procedures (HTTP requests use structured fields, not templates). A template receives just the step's resolved concrete inputs — **no** `{{ ledger. }}`, `{{ env. }}`, or `{{ data. }}` inside a `.njk` file.

```nunjucks
<invoice>
  <paymentId>{{ payment_id }}</paymentId>
  <amount>{{ amount }}</amount>
</invoice>
```

### 9.7 Input model — `models/<name>.yaml`

An optional, reusable declaration of a procedure's inputs, referenced by `input_model:`. Each field carries a `type` and `description`.

```yaml
name: validate_payment_created_input
fields:
  response:
    type: object
    description: "The response envelope from the send step"
  expected_status:
    type: number
    description: "The HTTP status code to assert"
```

### 9.8 `data.yaml` — plugin shared data

Optional. Holds example request bodies (one key per operation, written by `syz generate`) and any hand-authored constants. Referenced as `{{ <plugin>.data.<key>... }}` from procedures, scenarios, or workflow inputs.

```yaml
# Generated example bodies — one block per operation
send_create_payment:
  headers: { Content-Type: application/json }
  params: {}
  body:
    amount: 1
    currency: example-currency
    customer_id: example-customer

# Hand-authored constants coexist alongside the generated keys
currencies:
  supported: ["AUD", "CNY", "EUR", "GBP", "INR", "JPY", "USD"]
  default: "GBP"
```

### 9.9 Environment file — `environments/<name>.yaml`

Selected at run time by `syz run --env <name>` (the `<name>` matches the file's `name:` field). A scenario never names an environment, so the same scenarios run unchanged against any env.

```yaml
name: local
production: false                    # stored in report metadata; not enforced at runtime
allow_destructive_operations: true   # same — advisory metadata for your own CI to gate on

# One top-level block per plugin. The keys under it are whatever your procedures
# reference via {{ env.<plugin>.<...> }} — the shape is yours to define.
payments:
  send_create_payment:
    base_url: "https://api-staging.payments.example.com/v1"
    auth_token: "{{ os-env-var:PAYMENTS_AUTH_TOKEN }}"   # secret — see below
  send_get_payment_status:
    base_url: "https://api-staging.payments.example.com/v1"
    auth_token: "{{ os-env-var:PAYMENTS_AUTH_TOKEN }}"   # same var: one plugin = one credential
  timeout_ms: 5000                   # any plain constant you want to reference

# Optional: named connection handles shared across steps (see §4 → Connections)
connections:
  payments_api:
    execution_library: http
    lifecycle: ephemeral             # ephemeral (default) | keep_alive
    http:
      request:
        fields:  { base_url: "https://api-staging.payments.example.com/v1" }
        headers: { Authorization: "Bearer {{ os-env-var:PAYMENTS_AUTH_TOKEN }}" }
```

**`{{ os-env-var:NAME }}` — the only secret mechanism.** Any string field may inject `process.env[NAME]`, with inline interpolation (`"Bearer {{ os-env-var:TOKEN }}"`). Every resolved value is treated as a secret and shown as `[MASKED]` in every artifact — only the live outbound request carries the real value. If the variable is unset, the literal `{{ os-env-var:NAME }}` is preserved so `syz lint` reports it rather than sending an empty credential. Never put a literal secret in a committed connection block — that's a `LITERAL_CREDENTIAL_IN_CONNECTION` error. For non-secret OS config you *want* visible, use a plain literal or a `{{ env.x.y }}` cross-reference instead.

**Env-driven header names (advanced).** A header *name* — not just its value — can come from the environment, so one procedure sends `Authorization` against one gateway and a different name (or none) against another with no plugin edit. Template the **key** and **quote it** (an unquoted `{{` corrupts the name):

```yaml
# procedure
http: { request: { headers: { "{{ env.payments.send_create_payment.auth_header }}": "Bearer {{ env.payments.send_create_payment.auth_token }}" } } }
# environments/local.yaml → auth_header: Authorization
# environments/prod.yaml  → auth_header: null      # explicit null drops the header entirely
```

An env key that is simply **absent** (or an unset `os-env-var`) does *not* drop the header — it's an unresolved reference and fails the run (`UNRESOLVED_TEMPLATE`).

### 9.10 Configuring executors

Today the only executor is **HTTP**, selected with `execution_library: http` on a send procedure (§9.2). "Configuring" it means writing its `http.request.fields` / `headers` / `settings` — including `tls` for mTLS or self-signed certs, `proxy`, and `timeout_ms`. There are **no drivers to install for HTTP** — it's built in.

- **Connection identity** can live in several layers, deep-merged field-by-field under one nearest-wins rule — lowest to highest: **env `connections.<name>` < procedure `connection` < procedure `http` < scenario `connections.<name>` < step override** (i.e. `step > scenario > procedure > env`). So a plugin owns its base identity, a scenario can retarget it for one run, and a deployer overrides through env **only** where the procedure ships that field as a `{{ env.* }}` / `{{ os-env-var:* }}` reference. Ship a **named handle** when identity is shared across steps or must be reused/renamed; use plain `{{ env.* }}` references for single-procedure identity (what `syz generate` emits).
- **`--no-install` / driver provisioning.** Stateful executors on the roadmap (Kafka, SQL, SFTP, S3, …) need client libraries that `syz` provisions on demand by scanning the `execution_library` values your plugins declare — pure-JS drivers install silently, native/browser ones ask consent (`--yes` to accept non-interactively). `--no-install` (or `SYZ_NO_AUTO_INSTALL=1`) turns this off and fails fast with the exact `npm install` command. **An HTTP-only project pulls nothing extra, so these flags are no-ops for it today.**

### 9.11 `dependency.json` — installed plugins

Lives at `.syz/dependency.json` (committed). Two writers, cleanly split: `syz install` manages the `plugins` array — plugins pulled from git (they land in `.syz/plugins.installed/<name>/`, auto-gitignored), where `name` is the local alias and must match the source folder and `version` is a git tag — and `syz init` stamps `syz_version` with the CLI version that set up the workspace. Because the `SYZ-*` docs are regenerable and not committed, `syz_version` is the committed record of which `syz` authored your tests; it's what the "your reference docs may be out of date — run `syz init`" advisory compares against, so a teammate who clones and upgrades still gets the nudge.

```json
{
  "syz_version": "0.10.9",
  "plugins": [
    { "name": "payments", "git": "https://github.com/acme/payments-plugin.git", "version": "v1.2.0" }
  ]
}
```

---

## Glossary

| Term | Definition |
|---|---|
| **Procedure** | A reusable operation (send, validate, workflow, craft) defined in YAML and shipped by a plugin |
| **Step** | One entry in a scenario's `flow:` — the invocation site. A step invokes a procedure |
| **Agent** | The AI assistant you author with. Never a plugin artifact — see the note below |
| **Scenario** | A sequence of steps (send → validate → send) that tests a workflow |
| **Plugin** | A collection of procedures, models, templates, and data for one API/system |
| **Ledger** | The immutable log of every request, response, and assertion in a scenario |
| **Outcome** | The result of a step: `success`, `failure`, `error`, `exception`, `aborted` |
| **Payload** | The rendered request (POST body, query params, etc.) sent to an executor |
| **Executor** | A handler for a protocol (HTTP today; Kafka, SQL, SFTP, etc. on the roadmap) |
| **Template** | A Nunjucks file that renders a request payload from inputs |
| **Input Model** | A YAML schema defining fields for a request |
| **Assertion** | A check on a response (e.g., "status equals 200") |
| **Connection Handle** | A named, reusable connection to a stateful system (database, message queue) |
| **Context Variable** | A runtime variable stored across steps (`ctx.var.name`) |
| **Outline/Parameterised Test** | Running the same flow with different input rows |
| **Workflow** | A reusable sequence of steps (e.g., send → validate → get status) |
| **Environment Config** | YAML file with base URLs, credentials, and settings per environment |
| **Sensitive Field** | A field that's masked in logs (API key, password, token) |
| **RTM** | Requirements Traceability Matrix — maps each requirement to the tests that cover it |

> **Three words, three meanings.** SYZYGY keeps *agent*, *procedure*, and *step* strictly separate:
> an **agent** is the AI assistant you author with; a **procedure** is a reusable definition your
> plugin ships (`send` / `validate` / `workflow` / `craft`); a **step** is one entry in a scenario's
> `flow:`. *A step invokes a procedure.* Nothing in the execution path calls a model unless you ask
> it to (`crafter: ai`, `operator: ai`, `{{ ai: }}`) — your procedures are plain declarative YAML
> that `syz` runs deterministically.

---

## Quick Reference

### Installation & Setup

```bash
# Check Node.js version (must be 20 LTS or later)
node --version

# Install SYZYGY globally
npm install -g syzs-cli

# Verify installation
syz --version

# Create new project and initialise workspace
mkdir my-tests && cd my-tests
syz init
```

### Most Common Commands

```bash
# Generate a plugin from OpenAPI spec
syz generate plugin --name payments --from openapi:./api.yaml

# Run all tests
syz run --suite .syz/scenarios --env local

# Run specific test
syz run --suite .syz/scenarios/e2e/payment_flow.yaml --env local

# Run tests, stay on failure
syz run --suite .syz/scenarios --env local --continue-scenario-on-failure

# Run with live progress
syz run --suite .syz/scenarios --env local --live-stats

# Run against staging environment
syz run --suite .syz/scenarios --env staging

# Generate JUnit XML report
syz run --suite .syz/scenarios --env local --reporter junit --output ./results.xml

# View requirements coverage
syz rtm

# Validate syntax without running
syz lint --env local
```

### File Locations Cheat Sheet

```
.syz/
├── dossier/
│   ├── DOMAIN.yaml                    ← Domain requirements (yours — edit manually)
│   ├── TEST-STRATEGY.md               ← Test approach (create/edit manually)
│   └── SYZ-TESTING.md                 ← Tutorial (generated, read-only)
├── plugins/
│   └── payments/
│       ├── plugin.yaml
│       ├── PLUGIN-GUIDE.yaml           ← Plugin rules (create/edit manually — §2)
│       ├── procedures/                 ← send/validate/workflow/craft (§9.2–9.5)
│       ├── models/                     ← Input models (optional — §9.7)
│       ├── templates/                  ← Nunjucks .njk (craft procedures only — §9.6)
│       └── data.yaml                   ← Shared constants + example bodies (§9.8)
├── environments/
│   ├── local.yaml                      ← Local env config (create/edit)
│   ├── staging.yaml                    ← Staging env config
│   └── production.yaml                 ← Production env config
├── scenarios/
│   ├── CLAUDE.md                       ← Scenario authoring guide
│   ├── e2e/                            ← Test scenarios (create/edit)
│   ├── integration/
│   ├── component/
│   └── generated/                      ← Sample scenarios (generated)
├── suites/
│   └── ci-critical.suite.yaml          ← Test playlists (create/edit)
└── results/
    └── run-2026-07-04T10-53-00-000Z/
        ├── execution.json              ← Execution summary
        ├── index.html                  ← Interactive viewer
        └── <scenario-path>/
            ├── debug.log               ← Plain-text transcript
            └── ledger.json             ← Full machine-readable log
```

---

## Conclusion

You now have everything you need to:

1. **Capture** business requirements in DOMAIN.yaml and PLUGIN-GUIDE.yaml
2. **Generate** a complete plugin from an API spec
3. **Author** test scenarios in simple YAML
4. **Execute** tests with the `syz run` command
5. **Audit** results via execution.json, index.html, debug.log, and ledger.json
6. **Track** requirements coverage with `syz rtm`

SYZYGY lets you describe **what** to test without writing **how** to test it. The CLI and your AI assistant handle the implementation details, keeping you in control of the business logic.

**Next Steps:**

- Read `.syz/dossier/SYZ-TESTING.md` for scenario authoring best practices
- Read `.syz/dossier/SYZ-GUIDELINES.md` for plugin design patterns
- Ask your AI assistant (Claude, Copilot, etc.) to help author DOMAIN.yaml, PLUGIN-GUIDE.yaml, and scenarios
- Review generated index.html to understand request/response flow
- Set up CI/CD to run tests on every commit

Good luck!
