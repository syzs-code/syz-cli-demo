name: "Workflow — create and validate in one step"
level: e2e
tags: [workflow]
covers: REQ-PAYMENTS-PG-001
plugins:
  - { name: payments, version: "1.0.0" }
flow:
  - type: workflow
    agent: payments.create_and_validate
